---
title: Интеграция Slack с OpenClaw - Полное руководство по ботам, каналам и корпоративной настройке
description: Полное руководство по подключению OpenClaw к Slack. Охватывает Socket Mode, HTTP webhooks, Bot Token, политики DM, списки разрешённых каналов, потоки, слэш-команды и мультиворкспейс настройку.
slug: /slack-integration
tags: slack, интеграция, корпоративный, воркспейс, каналы, бот
publishedAt: 2025-02-15
status: published
visibility: public
featuredImageUrl: /images/features/slack-integration.webp
---

# Интеграция Slack с OpenClaw: Полное руководство по настройке

Добавьте вашего AI-ассистента в Slack и сотрудничайте с ним в каналах, личных сообщениях и тредах. OpenClaw глубоко интегрируется с моделью воркспейсов Slack — поддерживает Socket Mode для простой настройки, HTTP webhooks для продакшена и детальный контроль доступа для корпоративных сред.

Это руководство охватывает всё: от создания Slack-приложения до настройки прав каналов, поведения тредов, слэш-команд и мульти-воркспейс развёртывания.

## Как это работает

OpenClaw подключается к Slack через Slack API, используя один из режимов:

- **Socket Mode** (рекомендуется): WebSocket-соединение в реальном времени, публичный URL не требуется
- **HTTP Mode**: Events API webhooks для продакшен-развёртывания

Оба режима поддерживают DM, каналы, треды, реакции, загрузку файлов и слэш-команды.

## Предварительные требования

Перед началом:

- OpenClaw Gateway установлен и запущен
- Slack воркспейс с правами администратора
- Понимание модели каналов и DM в Slack

## Быстрый старт (Socket Mode)

### Шаг 1: Создание Slack-приложения

1. Перейдите на [api.slack.com/apps](https://api.slack.com/apps)
2. Нажмите **Create New App** → **From scratch**
3. Назовите "OpenClaw" и выберите ваш воркспейс

### Шаг 2: Включение Socket Mode

1. Перейдите в **Settings → Socket Mode**
2. Включите **Enable Socket Mode**
3. Перейдите в **Basic Information → App-Level Tokens**
4. Нажмите **Generate Token and Scopes**
5. Добавьте scope `connections:write`
6. Скопируйте **App Token** (`xapp-...`)

### Шаг 3: Настройка OAuth-прав

1. Перейдите в **OAuth & Permissions**
2. Добавьте эти **Bot Token Scopes**:
   - `chat:write` — отправка сообщений
   - `channels:history`, `channels:read` — чтение каналов
   - `im:history`, `im:read`, `im:write` — личные сообщения
   - `users:read` — информация о пользователях
   - `reactions:read`, `reactions:write` — реакции
   - `app_mentions:read` — @упоминания
   - `commands` — слэш-команды
3. Нажмите **Install to Workspace**
4. Скопируйте **Bot User OAuth Token** (`xoxb-...`)

### Шаг 4: Подписка на события

1. Перейдите в **Event Subscriptions**
2. Включите **Enable Events**
3. В **Subscribe to bot events** добавьте:
   - `message.channels`, `message.groups`, `message.im`, `message.mpim`
   - `app_mention`
   - `reaction_added`, `reaction_removed`

### Шаг 5: Настройка OpenClaw

Установите токены через переменные окружения:

```bash
export SLACK_APP_TOKEN="xapp-..."
export SLACK_BOT_TOKEN="xoxb-..."
```

Или в `~/.openclaw/openclaw.json`:

```json5
{
  channels: {
    slack: {
      enabled: true,
      appToken: "xapp-...",
      botToken: "xoxb-...",
    },
  },
}
```

### Шаг 6: Приглашение бота

Пригласите OpenClaw в каналы:

```
/invite @OpenClaw
```

## Контроль доступа к DM

### Режим сопряжения (по умолчанию, безопасный)

Неизвестным отправителям отправляется код сопряжения:

```json5
{
  channels: {
    slack: {
      dm: {
        policy: "pairing",
      },
    },
  },
}
```

**Одобрение отправителей:**

```bash
openclaw pairing list slack
openclaw pairing approve slack <CODE>
```

### Режим белого списка

Только определённые пользователи могут писать в DM:

```json5
{
  channels: {
    slack: {
      dm: {
        policy: "allowlist",
        allowFrom: ["U123ABC", "@jane"],
      },
    },
  },
}
```

## Настройка каналов

### Групповая политика

Контроль доступных для бота каналов:

```json5
{
  channels: {
    slack: {
      groupPolicy: "allowlist",
      channels: {
        "#general": { allow: true, requireMention: true },
        "#support": { allow: true, requireMention: false },
      },
    },
  },
}
```

## Треды ответов

Управление тредами бота:

```json5
{
  channels: {
    slack: {
      replyToMode: "off", // "off" | "first" | "all"
    },
  },
}
```

| Режим | Поведение |
|-------|-----------|
| `off` | Ответ в основном канале (по умолчанию) |
| `first` | Первый ответ в тред, последующие в канал |
| `all` | Все ответы в тред |

## Слэш-команды

```json5
{
  channels: {
    slack: {
      slashCommand: {
        enabled: true,
        name: "openclaw",
        ephemeral: true,
      },
    },
  },
}
```

## Полный пример конфигурации

```json5
{
  channels: {
    slack: {
      enabled: true,
      appToken: "xapp-...",
      botToken: "xoxb-...",
      
      dm: {
        policy: "pairing",
        allowFrom: ["U123ABC"],
      },
      
      groupPolicy: "allowlist",
      channels: {
        "#general": { allow: true, requireMention: true },
      },
      
      replyToMode: "first",
      historyLimit: 50,
      
      actions: {
        reactions: true,
        messages: true,
      },
    },
  },
}
```

## CLI-команды

```bash
# Проверка статуса
openclaw channels status --probe

# Отправка сообщения
openclaw message send --channel slack --to "user:U123" --message "Привет!"

# Просмотр логов
openclaw channels logs --channel slack
```

## Устранение неполадок

### Бот не отвечает

1. Проверьте правильность токенов
2. Убедитесь, что Socket Mode включён
3. Проверьте, что бот приглашён в канал

### Ошибка "missing scope"

Добавьте нужный scope в **OAuth & Permissions** и переустановите приложение.

## Итоги

Вы настроили интеграцию Slack:

- ✅ **Socket Mode** или HTTP webhooks
- ✅ **Политики DM** для контроля доступа
- ✅ **Списки разрешённых каналов** с ограничением по упоминанию
- ✅ **Треды** настройки
- ✅ **Слэш-команды** поддержка

Ваш AI-ассистент теперь доступен в Slack! 💼
