---
title: Интеграция WhatsApp с OpenClaw - Полное руководство по группам, медиа и мульти-аккаунтам
description: Полное руководство по подключению OpenClaw к WhatsApp через Baileys. Охватывает QR-логин, политики DM, групповые чаты, обработку медиа, режим сопряжения и настройку мульти-аккаунтов.
slug: /whatsapp-integration
tags: whatsapp, интеграция, baileys, сообщения, группы, медиа
publishedAt: 2025-02-15
status: published
visibility: public
featuredImageUrl: /images/features/whatsapp-integration.webp
---

# Интеграция WhatsApp с OpenClaw: Полное руководство по настройке

Подключите вашего AI-ассистента к WhatsApp и общайтесь с ним как с другом. OpenClaw использует WhatsApp Web (через Baileys) для обеспечения бесшовного обмена сообщениями — без бизнес-API, без 24-часовых ограничений на ответы, только чистая магия персонального ассистента.

Это руководство охватывает всё: от базового QR-входа до продвинутой настройки групповых чатов, обработки медиа и мульти-аккаунтов.

## Как это работает

OpenClaw подключается к WhatsApp Web с помощью библиотеки Baileys (та же технология, что стоит за многими WhatsApp-ботами). Ваш телефон выступает устройством аутентификации, а OpenClaw поддерживает постоянную сессию на компьютере.

**Преимущества перед WhatsApp Business API:**
- Нет ограничения 24-часового окна ответа
- Работает с личными номерами
- Не требует настройки Meta Business Manager
- Полная поддержка групповых чатов

## Предварительные требования

Перед началом:

- OpenClaw Gateway установлен и запущен
- Телефонный номер для WhatsApp (личный или выделенный)
- Среда Node.js (**не Bun** — Baileys нестабилен на Bun)

## Шаг 1: Базовая конфигурация

Добавьте WhatsApp в `~/.openclaw/openclaw.json`:

```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "allowlist",
      allowFrom: ["+79121234567"], // Ваш номер телефона (формат E.164)
    },
  },
}
```

**Важно:** Используйте формат E.164 для телефонных номеров (например, `+7` для России, `+1` для США).

## Шаг 2: Вход через QR-код

Свяжите ваш аккаунт WhatsApp:

```bash
openclaw channels login --channel whatsapp
```

В терминале отобразится QR-код. Отсканируйте его телефоном:

1. Откройте WhatsApp на телефоне
2. Перейдите в **Настройки → Связанные устройства**
3. Нажмите **Привязать устройство**
4. Отсканируйте QR-код

После связывания вы увидите подтверждение. Сессия сохраняется в `~/.openclaw/credentials/whatsapp/default/creds.json`.

## Шаг 3: Проверка подключения

Проверьте статус канала:

```bash
openclaw channels status --probe
```

WhatsApp должен отображаться как `linked: true` и `running`.

## Варианты телефонного номера

### Вариант A: Выделенный номер (Рекомендуется)

Используйте **отдельный телефонный номер** для OpenClaw. Преимущества:
- Чёткое разделение с личными сообщениями
- Нет риска путаницы контактов
- Легче устранять неполадки

**Лучшая настройка:** Старый Android-телефон + eSIM на Wi-Fi с WhatsApp Business.

### Вариант B: Личный номер (Запасной)

Запустите OpenClaw на **вашем личном номере WhatsApp**. Используйте «Сообщение себе» для тестирования. Включите режим самочата:

```json5
{
  channels: {
    whatsapp: {
      selfChatMode: true,
      dmPolicy: "allowlist",
      allowFrom: ["+79121234567"],
    },
  },
}
```

## Контроль доступа к DM

### Режим белого списка (По умолчанию, самый безопасный)

Только определённые номера могут писать боту:

```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "allowlist",
      allowFrom: ["+79121234567", "+79129876543"],
    },
  },
}
```

### Режим сопряжения (Одобрение новых отправителей)

Неизвестным отправителям отправляется код сопряжения. Одобрите вручную:

```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "pairing",
    },
  },
}
```

**Одобрение отправителя:**

```bash
openclaw pairing list whatsapp
openclaw pairing approve whatsapp <CODE>
```

### Открытый режим (Разрешить всем)

Разрешить всем писать боту (**используйте осторожно**):

```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "open",
      allowFrom: ["*"],
    },
  },
}
```

## Настройка групповых чатов

OpenClaw может участвовать в группах WhatsApp с умной активацией.

### Включение доступа к группам

```json5
{
  channels: {
    whatsapp: {
      groupPolicy: "allowlist",
      groups: {
        "*": { requireMention: true }, // Разрешить все группы, требуется @упоминание
      },
    },
  },
}
```

### Режимы активации

**Режим упоминания (по умолчанию):** Бот отвечает только при @упоминании:

```json5
{
  agents: {
    list: [
      {
        id: "main",
        groupChat: {
          mentionPatterns: ["@openclaw", "openclaw"],
          historyLimit: 50,
        },
      },
    ],
  },
}
```

**Режим «всегда»:** Бот отвечает на все сообщения (используйте умеренно):

```
/activation always
```

Переключение: `/activation mention` (только для владельца).

## Обработка медиа

### Получение медиа

OpenClaw скачивает и обрабатывает входящие медиа:

```json5
{
  channels: {
    whatsapp: {
      mediaMaxMb: 50,
    },
  },
}
```

### Отправка медиа

Бот может отправлять изображения, видео, аудио и документы:

```json5
{
  agents: {
    defaults: {
      mediaMaxMb: 5,
    },
  },
}
```

## Автоматические реакции

Отправка эмодзи-реакций при получении сообщений:

```json5
{
  channels: {
    whatsapp: {
      ackReaction: {
        emoji: "👀",
        direct: true,
        group: "mentions",
      },
    },
  },
}
```

## Настройка мульти-аккаунтов

Запуск нескольких аккаунтов WhatsApp в одном Gateway:

```json5
{
  channels: {
    whatsapp: {
      accounts: {
        personal: {
          allowFrom: ["+79111111111"],
        },
        work: {
          allowFrom: ["+79122222222"],
          ackReaction: { emoji: "✅" },
        },
      },
    },
  },
}
```

**Вход в определённый аккаунт:**

```bash
openclaw channels login --account personal
openclaw channels login --account work
```

## CLI-команды

```bash
# Проверка статуса
openclaw channels status --probe

# Отправка сообщений
openclaw message send --channel whatsapp --to "+79121234567" --message "Привет!"

# Просмотр логов
openclaw channels logs --channel whatsapp

# Выход
openclaw channels logout --channel whatsapp
```

## Устранение неполадок

### «Не связано» или требуется QR-вход

**Симптом:** `channels status` показывает `linked: false`

**Исправление:** Запустите `openclaw channels login` и повторно отсканируйте QR-код.

### Связано, но отключено / цикл переподключения

**Исправление:**
1. Запустите `openclaw doctor`
2. Перезапустите gateway
3. Если проблема сохраняется, переподключитесь через `openclaw channels login`

### Проблемы с Bun

**Исправление:** WhatsApp (Baileys) нестабилен на Bun. Переключитесь на Node.js:

```bash
export OPENCLAW_RUNTIME=node
```

## Итоги

Вы настроили интеграцию WhatsApp:

- ✅ **QR-вход** через WhatsApp Web
- ✅ **Политики DM** для контроля доступа
- ✅ **Поддержка групповых чатов** с активацией по упоминанию
- ✅ **Обработка медиа** — изображения, видео, голосовые сообщения
- ✅ **Автоматические реакции** для мгновенной обратной связи
- ✅ **Мульти-аккаунт** поддержка

Ваш AI-ассистент теперь доступен в WhatsApp! 💬
