---
title: "Как интегрировать LINE Messaging с вашим AI-чатботом"
description: "Пошаговое руководство по подключению агентов OpenClaw AI к LINE Messaging API. Включите интеллектуальную автоматизацию чата для ваших пользователей LINE с настройкой вебхука, аутентификацией и поддержкой расширенных сообщений."
slug: /line-integration
publishedAt: 2026-02-16
status: published
visibility: public
featuredImageUrl: /images/features/line-integration.webp
---

# Как интегрировать LINE Messaging с вашим AI-чатботом

LINE — одна из самых популярных платформ обмена сообщениями в Азии с более чем 200 миллионами активных пользователей в месяц в Японии, Тайване, Таиланде и Индонезии. Интеграция вашего AI-чатбота с LINE позволяет охватить эту огромную аудиторию с помощью интеллектуальных автоматизированных разговоров.

В этом руководстве вы узнаете, как подключить агенты OpenClaw AI к LINE Messaging API, позволяя вашему чатботу обрабатывать личные сообщения, групповые чаты, обмен медиафайлами и расширенные интерактивные сообщения.

## Что вы получите

После завершения этой интеграции ваш AI-ассистент сможет:

- **Отвечать пользователям LINE** в режиме реального времени с интеллектуальными разговорами
- **Обрабатывать групповые чаты** и личные сообщения без проблем
- **Делиться медиафайлами**, включая изображения, видео и местоположения
- **Отправлять расширенные сообщения** с использованием форматов Flex Message и шаблонов LINE
- **Предоставлять быстрые ответы** для лучшего пользовательского опыта
- **Поддерживать несколько аккаунтов LINE** из одного экземпляра OpenClaw

## Предварительные требования

Перед началом убедитесь, что у вас есть:

1. **OpenClaw установлен и работает** - Если вы еще не установили OpenClaw, следуйте [руководству по установке](/docs/getting-started).
2. **Аккаунт LINE Developers** - Зарегистрируйтесь на [LINE Developers Console](https://developers.line.biz/console/).
3. **HTTPS-эндпоинт** - Вебхуки LINE требуют HTTPS. Используйте обратный прокси (nginx, Caddy) или туннельный сервис (ngrok, Cloudflare Tunnel).
4. **Доступ к Gateway** - Убедитесь, что ваш OpenClaw gateway доступен из интернета.

## Шаг 1: Установите плагин LINE

OpenClaw использует плагинную архитектуру для каналов обмена сообщениями. Установите плагин LINE:

```bash
openclaw plugins install @openclaw/line
```

Если вы запускаете OpenClaw из git-репозитория, вы можете установить локальную версию:

```bash
openclaw plugins install ./extensions/line
```

## Шаг 2: Создайте канал LINE Messaging API

1. **Войдите в LINE Developers Console**  
   Посетите [https://developers.line.biz/console/](https://developers.line.biz/console/) и войдите с вашим аккаунтом LINE.

2. **Создайте Provider**  
   Если у вас еще нет Provider, создайте его. Provider представляет вашу компанию или сервис.

3. **Добавьте канал Messaging API**  
   - Нажмите **Создать новый канал**
   - Выберите **Messaging API** в качестве типа канала
   - Заполните необходимую информацию:
     - Название канала (например, "Мой AI-ассистент")
     - Описание канала
     - Категория и подкатегория
     - Адрес электронной почты
   - Согласитесь с условиями и нажмите **Создать**

4. **Получите учетные данные**  
   После создания канала перейдите на вкладки **Основные настройки** и **Messaging API**, чтобы найти:
   - **Channel access token** - Нажмите "Issue", чтобы сгенерировать долгосрочный токен
   - **Channel secret** - Находится на вкладке "Основные настройки"

   Храните эти учетные данные в безопасности - они понадобятся для настройки OpenClaw.

5. **Включите вебхуки**  
   На вкладке **Messaging API**:
   - Переключите **Использовать вебхук** в положение ВКЛ
   - Установите URL вебхука (мы настроим это на следующем шаге)
   - Отключите **Автоответы** и **Приветственные сообщения**, если хотите, чтобы AI обрабатывал все взаимодействия

## Шаг 3: Настройте URL вебхука

LINE должен отправлять входящие сообщения в ваш OpenClaw gateway. Путь вебхука по умолчанию:

```
https://your-gateway-domain.com/line/webhook
```

Замените `your-gateway-domain.com` на ваш фактический домен или IP-адрес gateway.

**Важно:** LINE требует HTTPS. Если вы тестируете локально, используйте туннельный сервис:

```bash
# Используя ngrok
ngrok http 3100

# Используя Cloudflare Tunnel
cloudflared tunnel --url http://localhost:3100
```

Обновите URL вебхука в LINE Developers Console вашим HTTPS-эндпоинтом.

## Шаг 4: Настройте OpenClaw

Добавьте конфигурацию канала LINE в ваш файл конфигурации OpenClaw (обычно `~/.openclaw/config.json5` или `config.json5` в вашем рабочем пространстве):

### Минимальная конфигурация

```json5
{
  channels: {
    line: {
      enabled: true,
      channelAccessToken: "YOUR_CHANNEL_ACCESS_TOKEN",
      channelSecret: "YOUR_CHANNEL_SECRET",
      dmPolicy: "pairing",
    },
  },
}
```

### Использование переменных окружения

Для лучшей безопасности используйте переменные окружения:

```bash
export LINE_CHANNEL_ACCESS_TOKEN="your_channel_access_token_here"
export LINE_CHANNEL_SECRET="your_channel_secret_here"
```

Затем ссылайтесь на них в вашей конфигурации:

```json5
{
  channels: {
    line: {
      enabled: true,
      // Token и secret будут прочитаны из переменных окружения
      dmPolicy: "pairing",
    },
  },
}
```

### Использование файлов токенов

Для продакшн-развертываний храните учетные данные в файлах:

```json5
{
  channels: {
    line: {
      enabled: true,
      tokenFile: "/secure/path/line-token.txt",
      secretFile: "/secure/path/line-secret.txt",
      dmPolicy: "pairing",
    },
  },
}
```

### Несколько аккаунтов LINE

Вы можете управлять несколькими каналами LINE из одного экземпляра OpenClaw:

```json5
{
  channels: {
    line: {
      accounts: {
        marketing: {
          channelAccessToken: "MARKETING_TOKEN",
          channelSecret: "MARKETING_SECRET",
          webhookPath: "/line/marketing",
        },
        support: {
          channelAccessToken: "SUPPORT_TOKEN",
          channelSecret: "SUPPORT_SECRET",
          webhookPath: "/line/support",
        },
      },
    },
  },
}
```

Не забудьте обновить URL вебхуков в консоли LINE в соответствии с вашими пользовательскими путями!

## Шаг 5: Настройте контроль доступа

Интеграция LINE поддерживает несколько политик доступа для контроля того, кто может взаимодействовать с вашим ботом:

### Режим сопряжения (рекомендуется)

Это значение по умолчанию и наиболее безопасный вариант. Новые пользователи должны быть одобрены, прежде чем они смогут общаться:

```json5
{
  channels: {
    line: {
      dmPolicy: "pairing",
    },
  },
}
```

Когда новый пользователь отправит сообщение вашему боту, он получит код сопряжения. Одобрите их, используя:

```bash
# Список ожидающих запросов на сопряжение
openclaw pairing list line

# Одобрить пользователя
openclaw pairing approve line <CODE>
```

### Режим белого списка

Только указанные ID пользователей LINE могут взаимодействовать с вашим ботом:

```json5
{
  channels: {
    line: {
      dmPolicy: "allowlist",
      allowFrom: [
        "U1234567890abcdef1234567890abcdef",
        "Uabcdef1234567890abcdef1234567890",
      ],
    },
  },
}
```

### Открытый режим

Любой может отправить сообщение вашему боту (используйте с осторожностью):

```json5
{
  channels: {
    line: {
      dmPolicy: "open",
    },
  },
}
```

### Политики групповых чатов

Настройте доступ для групповых чатов LINE отдельно:

```json5
{
  channels: {
    line: {
      groupPolicy: "allowlist",
      groupAllowFrom: [
        "U1234567890abcdef1234567890abcdef", // Пользователь, который может добавлять бота в группы
      ],
      groups: {
        "C1234567890abcdef1234567890abcdef": {
          // Переопределения для конкретной группы
          allowFrom: ["Uabcdef1234567890abcdef1234567890"],
        },
      },
    },
  },
}
```

**Примечание:** ID LINE чувствительны к регистру и следуют этим шаблонам:
- ID пользователя: `U` + 32 шестнадцатеричных символа
- ID группы: `C` + 32 шестнадцатеричных символа
- ID комнаты: `R` + 32 шестнадцатеричных символа

## Шаг 6: Перезапустите и протестируйте

Перезапустите ваш OpenClaw gateway, чтобы применить конфигурацию:

```bash
openclaw gateway restart
```

Теперь протестируйте интеграцию:

1. **Добавьте вашего бота в друзья** - Отсканируйте QR-код или найдите по LINE ID в LINE Developers Console
2. **Отправьте тестовое сообщение** - Введите "Привет" или любое сообщение вашему боту
3. **Проверьте ответ** - Если сопряжение включено, вы сначала получите код сопряжения

Отслеживайте логи gateway для событий вебхука:

```bash
openclaw gateway logs -f
```

Вы должны увидеть входящие события вебхука LINE и исходящие ответы.

## Расширенные функции

### Расширенные сообщения с Flex Cards

LINE поддерживает мощные расширенные сообщения, называемые Flex Messages. Отправляйте интерактивные карточки, используя `channelData`:

```json5
{
  text: "Вот статус вашего заказа",
  channelData: {
    line: {
      flexMessage: {
        altText: "Карточка статуса заказа",
        contents: {
          type: "bubble",
          body: {
            type: "box",
            layout: "vertical",
            contents: [
              {
                type: "text",
                text: "Заказ #12345",
                weight: "bold",
                size: "xl",
              },
              {
                type: "text",
                text: "Статус: Отправлено",
                color: "#00B900",
              },
            ],
          },
        },
      },
    },
  },
}
```

### Быстрые ответы

Предоставьте кнопки быстрых ответов для лучшего UX:

```json5
{
  text: "Что бы вы хотели сделать?",
  channelData: {
    line: {
      quickReplies: ["Проверить статус", "Связаться с поддержкой", "Посмотреть историю"],
    },
  },
}
```

### Обмен местоположением

Отправьте информацию о местоположении:

```json5
{
  text: "Вот местоположение нашего офиса",
  channelData: {
    line: {
      location: {
        title: "Штаб-квартира OpenClaw",
        address: "улица AI 123, Технологический город",
        latitude: 35.681236,
        longitude: 139.767125,
      },
    },
  },
}
```

### Обработка медиафайлов

Плагин LINE автоматически обрабатывает загрузку и скачивание медиафайлов. Настройте ограничение размера:

```json5
{
  channels: {
    line: {
      mediaMaxMb: 20, // По умолчанию 10 МБ
    },
  },
}
```

## Устранение неполадок

### Проверка вебхука не удалась

**Проблема:** Консоль LINE показывает "Проверка вебхука не удалась"

**Решения:**
- Убедитесь, что URL вебхука использует HTTPS (а не HTTP)
- Проверьте, что `channelSecret` в вашей конфигурации точно соответствует консоли LINE
- Проверьте, что ваш gateway доступен из интернета
- Протестируйте эндпоинт вручную: `curl https://your-gateway/line/webhook`

### Нет входящих сообщений

**Проблема:** Бот не получает сообщения от пользователей LINE

**Решения:**
- Подтвердите, что путь вебхука соответствует `channels.line.webhookPath` в вашей конфигурации
- Проверьте логи gateway на наличие POST-запросов вебхука: `openclaw gateway logs -f`
- Убедитесь, что URL вебхука включен в LINE Developers Console
- Убедитесь, что автоответ отключен в консоли LINE (он мешает доставке вебхука)

### Ошибки скачивания медиафайлов

**Проблема:** Не удается скачать изображения или видео

**Решения:**
- Увеличьте `channels.line.mediaMaxMb`, если файлы превышают 10 МБ
- Проверьте права gateway на запись во временные каталоги
- Убедитесь, что ваш токен доступа к каналу LINE имеет права на скачивание медиафайлов

### Сообщения не отправляются

**Проблема:** Бот получает сообщения, но не может отправлять ответы

**Решения:**
- Проверьте, что `channelAccessToken` действителен и не истек
- Проверьте, что токен имеет право "SEND_MESSAGE" в консоли LINE
- Просмотрите логи gateway на наличие ответов об ошибках API
- Убедитесь, что пользователь не заблокировал вашего бота

### Одобрение сопряжения не работает

**Проблема:** Невозможно одобрить запросы на сопряжение

**Решения:**
- Запустите `openclaw pairing list line`, чтобы увидеть ожидающие запросы
- Убедитесь, что код сопряжения правильный (с учетом регистра)
- Проверьте, что `dmPolicy` установлен на `"pairing"` в конфигурации
- Перезапустите gateway после одобрения: `openclaw gateway restart`

## Резюме

Поздравляем! Вы успешно интегрировали LINE Messaging с вашим чатботом OpenClaw AI. Ваш бот теперь может:

✅ Получать и отвечать на сообщения LINE  
✅ Обрабатывать групповые чаты и личные сообщения  
✅ Отправлять расширенный интерактивный контент с Flex Messages  
✅ Делиться медиафайлами, местоположениями и быстрыми ответами  
✅ Контролировать доступ с помощью сопряжения, белого списка или открытых политик  
✅ Управлять несколькими аккаунтами LINE из одного gateway  

### Следующие шаги

- **Настройте ответы** - Настройте личность и поведение вашего агента
- **Добавьте пользовательские команды** - Создайте команды для LINE с `/card` и другими инструментами
- **Отслеживайте использование** - Настройте аналитику и логирование для взаимодействий LINE
- **Изучите интеграции** - Подключите LINE к другим каналам, таким как Discord, Slack или WhatsApp

### Полезные ресурсы

- [Документация LINE Messaging API](https://developers.line.biz/en/docs/messaging-api/)
- [Документация каналов OpenClaw](/docs/channels)
- [Симулятор Flex Message](https://developers.line.biz/flex-simulator/)
- [LINE Developers Console](https://developers.line.biz/console/)

Счастливого общения! 🤖💚
