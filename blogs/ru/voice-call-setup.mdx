---
title: Настройка голосовых звонков в OpenClaw - Twilio, STT и TTS
description: Полное руководство по настройке голосовых звонков в OpenClaw с интеграцией Twilio, распознаванием речи (STT) через Deepgram/Whisper и синтезом речи (TTS) через OpenAI/ElevenLabs.
slug: /voice-call-setup
tags: голосовые-звонки, twilio, tts, stt, deepgram, elevenlabs, openai, телефония
publishedAt: 2025-02-15
status: published
visibility: public
featuredImageUrl: /images/features/voice-call-setup.webp
---

# Настройка голосовых звонков в OpenClaw: Twilio, STT и TTS

Хотите, чтобы ваш AI-ассистент совершал и принимал телефонные звонки? Плагин Voice Call для OpenClaw обеспечивает реальные голосовые разговоры через Twilio с профессиональной интеграцией распознавания речи (STT) и синтеза речи (TTS).

Это руководство проведёт вас через полную настройку: от установки плагина до настройки вебхуков Twilio, провайдеров STT (Deepgram, OpenAI Whisper, Groq) и голосов TTS (OpenAI, ElevenLabs).

## Предварительные требования

Перед началом убедитесь, что у вас есть:

- Работающий OpenClaw Gateway (v1.0+)
- Аккаунт Twilio с телефонным номером
- API-ключи выбранных провайдеров STT/TTS
- Публично доступный URL для вебхука (через ngrok, Tailscale или VPS)

## Шаг 1: Установка плагина Voice Call

Плагин Voice Call работает внутри процесса Gateway. Установите его с помощью OpenClaw CLI:

```bash
openclaw plugins install @openclaw/voice-call
```

После установки перезапустите Gateway:

```bash
openclaw gateway restart
```

Проверьте, что плагин загружен:

```bash
openclaw status
```

Вы должны увидеть `voice-call` в списке активных плагинов.

## Шаг 2: Настройка Twilio

Сначала получите учётные данные из [консоли Twilio](https://console.twilio.com):

- **Account SID**: Найдите на панели управления
- **Auth Token**: Найдите на панели управления
- **Телефонный номер**: Номер Twilio с возможностью голосовых вызовов

Добавьте конфигурацию Twilio в `openclaw.json`:

```json5
\{
  plugins: \{
    entries: \{
      "voice-call": \{
        enabled: true,
        config: \{
          provider: "twilio",
          fromNumber: "+15550001234", // Ваш номер Twilio
          toNumber: "+15550005678",   // Получатель по умолчанию (необязательно)
          
          twilio: \{
            accountSid: "ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
            authToken: "your_auth_token_here",
          \},
        \},
      \},
    \},
  \},
\}
```

## Шаг 3: Настройка вебхуков

Twilio должен иметь доступ к вашему экземпляру OpenClaw для доставки событий звонков. У вас есть три варианта:

### Вариант A: ngrok (Разработка)

Для локальной разработки используйте ngrok:

```bash
ngrok http 3334
```

Затем укажите публичный URL в конфигурации:

```json5
\{
  plugins: \{
    entries: \{
      "voice-call": \{
        config: \{
          publicUrl: "https://abc123.ngrok.app/voice/webhook",
          
          serve: \{
            port: 3334,
            path: "/voice/webhook",
          \},
        \},
      \},
    \},
  \},
\}
```

### Вариант B: Tailscale Funnel (Рекомендуется)

Tailscale Funnel предоставляет стабильную и безопасную конечную точку:

```bash
openclaw voicecall expose --mode funnel
```

Настройте в `openclaw.json`:

```json5
\{
  plugins: \{
    entries: \{
      "voice-call": \{
        config: \{
          tailscale: \{
            mode: "funnel",
            path: "/voice/webhook",
          \},
          
          serve: \{
            port: 3334,
            path: "/voice/webhook",
          \},
        \},
      \},
    \},
  \},
\}
```

### Вариант C: VPS с обратным прокси

Для продакшена используйте VPS с nginx:

```json5
\{
  plugins: \{
    entries: \{
      "voice-call": \{
        config: \{
          publicUrl: "https://voice.example.com/voice/webhook",
          
          webhookSecurity: \{
            allowedHosts: ["voice.example.com"],
            trustedProxyIPs: ["100.64.0.1"],
          \},
          
          serve: \{
            port: 3334,
            path: "/voice/webhook",
          \},
        \},
      \},
    \},
  \},
\}
```

## Шаг 4: Настройка распознавания речи (STT)

STT преобразует входящую речь в текст. OpenClaw поддерживает несколько провайдеров с автоматическим переключением.

### Вариант A: Deepgram (Рекомендуется)

Deepgram предлагает отличную точность и скорость:

```json5
\{
  tools: \{
    media: \{
      audio: \{
        enabled: true,
        models: [\{ provider: "deepgram", model: "nova-3" \}],
        providerOptions: \{
          deepgram: \{
            detect_language: true,
            punctuate: true,
            smart_format: true,
          \},
        \},
      \},
    \},
  \},
\}
```

Установите API-ключ:

```bash
export DEEPGRAM_API_KEY="dg_xxxxxxxxxxxxxxxx"
```

### Вариант B: OpenAI Whisper

Для интеграции с OpenAI:

```json5
\{
  tools: \{
    media: \{
      audio: \{
        enabled: true,
        models: [\{ provider: "openai", model: "gpt-4o-mini-transcribe" \}],
      \},
    \},
  \},
\}
```

Или используйте модель с более высокой точностью:

```json5
\{
  tools: \{
    media: \{
      audio: \{
        models: [\{ provider: "openai", model: "gpt-4o-transcribe" \}],
      \},
    \},
  \},
\}
```

### Вариант C: Groq (Быстро и доступно)

Groq предлагает быстрый инференс Whisper:

```json5
\{
  tools: \{
    media: \{
      audio: \{
        enabled: true,
        models: [\{ provider: "groq", model: "whisper-large-v3-turbo" \}],
      \},
    \},
  \},
\}
```

### Вариант D: Локальный Whisper CLI (Офлайн)

Для конфиденциальности используйте локальный Whisper:

```json5
\{
  tools: \{
    media: \{
      audio: \{
        enabled: true,
        models: [
          \{
            type: "cli",
            command: "whisper",
            args: ["--model", "base", "\{\{MediaPath\}\}"],
            timeoutSeconds: 45,
          \},
        ],
      \},
    \},
  \},
\}
```

### Мультипровайдерное переключение

Настройте несколько провайдеров для надёжности:

```json5
\{
  tools: \{
    media: \{
      audio: \{
        enabled: true,
        maxBytes: 20971520,
        models: [
          \{ provider: "openai", model: "gpt-4o-mini-transcribe" \},
          \{ provider: "deepgram", model: "nova-3" \},
          \{
            type: "cli",
            command: "whisper",
            args: ["--model", "base", "\{\{MediaPath\}\}"],
            timeoutSeconds: 45,
          \},
        ],
      \},
    \},
  \},
\}
```

## Шаг 5: Настройка синтеза речи (TTS)

TTS преобразует ответы AI в речь во время звонков. Voice Call использует основную конфигурацию `messages.tts` с опциональным переопределением.

### Вариант A: OpenAI TTS

Чистые, естественные голоса:

```json5
\{
  messages: \{
    tts: \{
      auto: "always",
      provider: "openai",
      openai: \{
        model: "gpt-4o-mini-tts",
        voice: "alloy", // Варианты: alloy, echo, fable, onyx, nova, shimmer
      \},
    \},
  \},
\}
```

### Вариант B: ElevenLabs (Премиум голоса)

Для сверхреалистичных голосов:

```json5
\{
  messages: \{
    tts: \{
      auto: "always",
      provider: "elevenlabs",
      elevenlabs: \{
        apiKey: "your_elevenlabs_key",
        voiceId: "pMsXgVXv3BLzUgSXRplE",
        modelId: "eleven_multilingual_v2",
        voiceSettings: \{
          stability: 0.5,
          similarityBoost: 0.75,
          speed: 1.0,
        \},
      \},
    \},
  \},
\}
```

### Переопределение TTS только для звонков

Используйте разные голоса для звонков и чата:

```json5
\{
  messages: \{
    tts: \{
      auto: "always",
      provider: "openai",
      openai: \{ voice: "nova" \},
    \},
  \},
  
  plugins: \{
    entries: \{
      "voice-call": \{
        config: \{
          tts: \{
            provider: "elevenlabs",
            elevenlabs: \{
              voiceId: "pMsXgVXv3BLzUgSXRplE",
              modelId: "eleven_v3",
            \},
          \},
        \},
      \},
    \},
  \},
\}
```

**Важно**: Edge TTS **не поддерживается** для голосовых звонков (телефония требует формат PCM).

## Шаг 6: Включение входящих звонков

По умолчанию входящие звонки отключены. Чтобы разрешить входящие:

```json5
\{
  plugins: \{
    entries: \{
      "voice-call": \{
        config: \{
          inboundPolicy: "allowlist",
          allowFrom: ["+15550001234", "+15550005678"],
          inboundGreeting: "Здравствуйте! Я ваш AI-ассистент. Чем могу помочь?",
        \},
      \},
    \},
  \},
\}
```

## Шаг 7: Включение медиа-стриминга

Для взаимодействия в реальном времени включите Twilio Media Streams:

```json5
\{
  plugins: \{
    entries: \{
      "voice-call": \{
        config: \{
          streaming: \{
            enabled: true,
            streamPath: "/voice/stream",
          \},
        \},
      \},
    \},
  \},
\}
```

## Полный пример конфигурации

Вот полная конфигурация, объединяющая всё вместе:

```json5
\{
  // Конфигурация STT
  tools: \{
    media: \{
      audio: \{
        enabled: true,
        maxBytes: 20971520,
        models: [
          \{ provider: "deepgram", model: "nova-3" \},
          \{ provider: "openai", model: "gpt-4o-mini-transcribe" \},
        ],
        providerOptions: \{
          deepgram: \{
            detect_language: true,
            punctuate: true,
          \},
        \},
      \},
    \},
  \},
  
  // Конфигурация TTS
  messages: \{
    tts: \{
      auto: "always",
      provider: "openai",
      openai: \{
        model: "gpt-4o-mini-tts",
        voice: "alloy",
      \},
    \},
  \},
  
  // Плагин Voice Call
  plugins: \{
    entries: \{
      "voice-call": \{
        enabled: true,
        config: \{
          provider: "twilio",
          fromNumber: "+15550001234",
          
          twilio: \{
            accountSid: "ACxxxxxxxx",
            authToken: "your_auth_token",
          \},
          
          serve: \{
            port: 3334,
            path: "/voice/webhook",
          \},
          
          publicUrl: "https://voice.example.com/voice/webhook",
          
          webhookSecurity: \{
            allowedHosts: ["voice.example.com"],
          \},
          
          outbound: \{
            defaultMode: "conversation",
          \},
          
          inboundPolicy: "allowlist",
          allowFrom: ["+15550001234"],
          inboundGreeting: "Здравствуйте! Чем могу помочь?",
          
          streaming: \{
            enabled: true,
            streamPath: "/voice/stream",
          \},
          
          // Опционально: переопределение TTS для звонков
          tts: \{
            provider: "elevenlabs",
            elevenlabs: \{
              voiceId: "pMsXgVXv3BLzUgSXRplE",
              modelId: "eleven_multilingual_v2",
            \},
          \},
        \},
      \},
    \},
  \},
\}
```

## Совершите первый звонок

Протестируйте настройку через CLI:

```bash
# Исходящий звонок
openclaw voicecall call --to "+15555550123" --message "Привет! Это тестовый звонок от OpenClaw."

# Проверка статуса звонка
openclaw voicecall status --call-id <call-id>

# Мониторинг звонков в реальном времени
openclaw voicecall tail
```

Или используйте инструмент агента в разговорах:

```
Вы: Позвони Ивану и напомни о завтрашней встрече.
Agent: [Использует инструмент voice_call для инициации звонка]
```

## Устранение неполадок

### Вебхук не получает события

1. Проверьте, что `publicUrl` доступен из интернета
2. Убедитесь, что верификация подписи Twilio проходит
3. Для ngrok убедитесь, что URL точно совпадает (следите за завершающим слэшем)

### Плохое качество звука

1. Убедитесь, что используете OpenAI или ElevenLabs (не Edge TTS)
2. Проверьте сетевую задержку до провайдеров TTS
3. Включите медиа-стриминг для взаимодействия в реальном времени

### STT не работает

1. Проверьте правильность установки API-ключа провайдера
2. Убедитесь, что `tools.media.audio.enabled: true`
3. Попробуйте цепочку переключения: Deepgram → OpenAI → Groq → Локальный Whisper

### Звонок сразу обрывается

1. Проверьте логи ошибок в консоли Twilio
2. Убедитесь, что вебхук возвращает валидный TwiML
3. Проверьте, что у номера Twilio есть возможность голосовых вызовов

## Следующие шаги

- Настройте [многоходовые разговоры](/blog/multi-agent-setup) с сохранением контекста
- Настройте [запланированные звонки](/blog/cron-job-best-practices) с помощью cron-задач
- Изучите альтернативных провайдеров: Telnyx, Plivo

## Итоги

Вы настроили голосовые звонки для OpenClaw:

- ✅ **Twilio** для телефонной инфраструктуры
- ✅ **STT** через Deepgram, OpenAI Whisper или Groq
- ✅ **TTS** через OpenAI или ElevenLabs
- ✅ **Вебхуки** для обработки звонков в реальном времени
- ✅ **Политики входящих** для приёма звонков

Ваш AI-ассистент теперь может разговаривать по телефону! 🎉
