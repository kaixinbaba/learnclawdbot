---
title: "Полное руководство: Установка OpenClaw на Windows, macOS и Linux"
description: "Пошаговое руководство по установке OpenClaw на всех основных платформах. Решение распространённых проблем с Node.js, npm и зависимостями"
slug: /troubleshooting/openclaw-setup-installation-guide
tags: installation, setup, troubleshooting, node.js, cross-platform
publishedAt: 2026-02-12
status: published
visibility: public
isPinned: false
featuredImageUrl: /images/features/openclaw-setup-guide.webp
---

# Полное руководство: Установка OpenClaw на Windows, macOS и Linux

Запуск OpenClaw на вашей системе должен быть простым, но разные операционные системы имеют свои особенности. Это руководство проведёт вас через установку на **Windows**, **macOS** и **Linux**, охватывая распространённые ошибки и их исправления.

## Предварительные требования: Что вам нужно сначала

Перед установкой OpenClaw убедитесь, что у вас есть:

### 1. Node.js (v18 или выше)

OpenClaw требует современную среду выполнения Node.js. Проверьте вашу версию:

```bash
node --version
```

Если у вас нет Node.js или установлена старая версия, установите:

**macOS (с использованием Homebrew):**
```bash
brew install node
```

**Windows (с использованием winget):**
```powershell
winget install OpenJS.NodeJS
```

**Linux (Ubuntu/Debian):**
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**Linux (Fedora/RHEL):**
```bash
sudo dnf install nodejs
```

### 2. npm или pnpm

npm поставляется с Node.js, но pnpm быстрее и эффективнее:

```bash
npm install -g pnpm
```

## Установка OpenClaw

### Шаг 1: Установка через npm

```bash
npm install -g @anthropic-ai/openclaw
```

Или с pnpm:

```bash
pnpm add -g @anthropic-ai/openclaw
```

### Шаг 2: Проверка установки

```bash
openclaw --version
```

Вы должны увидеть номер версии (например, `0.5.2`).

## Настройка для конкретной платформы

### 🪟 Windows

#### Проблема №1: Политика выполнения PowerShell

**Симптом:** `openclaw: Не удается загрузить файл... выполнение скриптов отключено в этой системе`

**Исправление:**
```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

#### Проблема №2: Поддержка длинных путей

Windows имеет ограничение длины пути в 260 символов, что может нарушить установку npm.

**Исправление:** Включите длинные пути в реестре
```powershell
New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
```

Или используйте WSL2 (подсистема Windows для Linux):
```powershell
wsl --install
```

Затем установите OpenClaw внутри WSL:
```bash
# Внутри WSL
npm install -g @anthropic-ai/openclaw
```

#### Проблема №3: Ошибки компиляции Node-gyp

**Симптом:** Ошибки во время установки об отсутствии инструментов сборки

**Исправление:** Установите инструменты сборки Windows
```powershell
npm install -g windows-build-tools
```

Или установите Visual Studio Build Tools вручную от Microsoft.

### 🍎 macOS

#### Проблема №1: Отказано в доступе

**Симптом:** `EACCES: permission denied` при запуске `npm install -g`

**Исправление:** Никогда не используйте `sudo` с npm. Вместо этого настройте npm для использования пользовательского каталога:

```bash
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.zshrc
source ~/.zshrc
```

Затем переустановите:
```bash
npm install -g @anthropic-ai/openclaw
```

#### Проблема №2: Команда не найдена после установки

**Симптом:** `openclaw: command not found` даже после успешной установки

**Исправление:** Проверьте, находится ли глобальный bin npm в PATH:
```bash
echo $PATH | grep npm
```

Если отсутствует, добавьте в `~/.zshrc`:
```bash
export PATH="$(npm config get prefix)/bin:$PATH"
```

#### Проблема №3: Требуется Rosetta (Mac M1/M2)

Некоторые зависимости могут требовать Rosetta 2 на Apple Silicon:

```bash
softwareupdate --install-rosetta
```

### 🐧 Linux

#### Проблема №1: EACCES при глобальной установке

**Симптом:** Ошибки разрешений, как в macOS

**Исправление:** То же решение - используйте пользовательский каталог:
```bash
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
npm install -g @anthropic-ai/openclaw
```

#### Проблема №2: Отсутствующие зависимости (Ubuntu/Debian)

**Симптом:** Ошибки сборки для нативных модулей

**Исправление:**
```bash
sudo apt-get update
sudo apt-get install -y build-essential python3
```

#### Проблема №3: Проблемы с Node.js из Snap

Если вы установили Node.js через Snap, могут возникнуть проблемы с разрешениями.

**Исправление:** Удалите версию Snap и используйте официальный репозиторий:
```bash
sudo snap remove node
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

## Первичная настройка

После установки настройте OpenClaw:

### 1. Установите API-ключ

```bash
openclaw configure
```

Следуйте подсказкам, чтобы добавить ваш API-ключ Anthropic.

### 2. Запустите Gateway

```bash
openclaw gateway start
```

### 3. Протестируйте настройку

```bash
openclaw chat "Привет, OpenClaw!"
```

Если вы видите ответ, поздравляем! 🎉

## Распространённые проблемы после установки

### Gateway не запускается

**Проверьте логи:**
```bash
tail -f ~/.openclaw/logs/gateway.log
```

**Обычные исправления:**
- Порт 3000 уже используется → Измените порт в конфигурации
- Отсутствует файл конфигурации → Запустите `openclaw configure` снова
- Блокировка файерволом → Разрешите Node.js в настройках файервола

### Устаревшая версия

Обновите OpenClaw:
```bash
npm update -g @anthropic-ai/openclaw
```

Или с pnpm:
```bash
pnpm update -g @anthropic-ai/openclaw
```

### Полная переустановка

Если всё остальное не помогло:
```bash
# Удаление
npm uninstall -g @anthropic-ai/openclaw

# Очистка кэша
npm cache clean --force

# Переустановка
npm install -g @anthropic-ai/openclaw
```

## Сравнение платформ

| Функция | Windows | macOS | Linux |
|---------|---------|-------|-------|
| Нативный терминал | ❌ Используйте WSL2 | ✅ Отлично | ✅ Отлично |
| Скорость установки | 🐢 Медленно | 🚀 Быстро | 🚀 Быстро |
| Требуются инструменты сборки | ⚠️ Часто | 🟢 Редко | ⚠️ Иногда |
| Рекомендуемая настройка | WSL2 + Ubuntu | Нативно | Нативно |

## Следующие шаги

После установки изучите:
- **[Настройка провайдеров](/providers/anthropic)** - Настройте Claude, OpenAI, Google
- **[Настройка Heartbeat](/troubleshooting/fix-cron-heartbeat-not-working)** - Включите запланированные задачи
- **[Основы терминала](/how-to-open-terminal)** - Изучите команды терминала

## Всё ещё есть проблемы?

Если вы застряли:
1. Проверьте [GitHub Issues](https://github.com/anthropics/claude-quickstarts/issues)
2. Просмотрите [официальную документацию](https://docs.openclaw.ai)
3. Присоединяйтесь к сообществу Discord для живой помощи

---

**Совет профессионала:** На Windows WSL2 обеспечивает лучший опыт работы с OpenClaw. Это как запуск Linux внутри Windows с полной мощью терминала.
