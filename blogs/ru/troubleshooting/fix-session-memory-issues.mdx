---
title: "Исправление проблем с памятью сессий и управлением контекстом в OpenClaw"
description: "Узнайте, как диагностировать и исправить проблемы сжатия сессий, потери памяти и контекстного окна в OpenClaw."
slug: /troubleshooting/fix-session-memory-issues
publishedAt: 2026-02-13
updatedAt: 2026-02-13
status: published
visibility: public
featuredImageUrl: /images/blog/fix-session-memory-issues.png
---

# Исправление проблем с памятью сессий и управлением контекстом в OpenClaw

Память сессий и управление контекстом являются критически важными компонентами архитектуры OpenClaw. Когда возникают проблемы—переполнение контекстного окна, потеря важной информации во время сжатия или неконтролируемое разрастание сессий—это может значительно повлиять на производительность и надежность вашего агента. Это руководство поможет вам диагностировать и исправить эти проблемы.

## Понимание архитектуры сессий OpenClaw

### Двухуровневая система персистентности

OpenClaw поддерживает состояние сессий через два различных уровня:

1. **Хранилище сессий (`sessions.json`)**
   - Карта ключ-значение, хранящая метаданные сессий
   - Расположение: `~/.openclaw/agents/<agentId>/sessions/sessions.json`
   - Отслеживает: ID сессий, последнюю активность, переключатели, счетчики токенов, количество сжатий
   - Безопасно для ручного редактирования

2. **Транскрипт сессии (`<sessionId>.jsonl`)**
   - JSONL-файл только для добавления, содержащий полное дерево разговоров
   - Расположение: `~/.openclaw/agents/<agentId>/sessions/<sessionId>.jsonl`
   - Хранит: сообщения, вызовы инструментов, сводки сжатий, точки ветвления
   - Используется для восстановления контекста для будущих ходов

**Критически важный принцип**: Gateway является источником истины. В удаленном режиме эти файлы находятся на удаленном хосте, а не на вашей локальной машине.

### Как работают ключи сессий

OpenClaw использует `sessionKey` для маршрутизации и изоляции разговоров:

- **Основной/прямой чат**: `agent:<agentId>:main` (по умолчанию)
- **Групповой чат**: `agent:<agentId>:<channel>:group:<id>`
- **Комнаты Discord/Slack**: `agent:<agentId>:<channel>:channel:<id>`
- **Cron-задачи**: `cron:<job.id>`
- **Вебхуки**: `hook:<uuid>`

Используйте `/status` в любом чате, чтобы увидеть текущий ключ сессии и использование контекста.

## Понимание контекстных окон и сжатия

### Что такое сжатие?

Каждая модель имеет **контекстное окно**—максимальное количество токенов, которое она может обработать за один запрос. Длительные разговоры накапливают сообщения и результаты инструментов, в конечном итоге приближаясь к этому лимиту. Функция **сжатия** OpenClaw суммирует старую историю разговоров в компактную сводку, сохраняя только недавние сообщения нетронутыми.

Сжатие является **персистентным**: сводка хранится в транскрипте сессии, поэтому будущие запросы видят:
- Сводку сжатия
- Недавние сообщения после точки сжатия

### Когда срабатывает автоматическое сжатие

Встроенный агент Pi в OpenClaw запускает автоматическое сжатие в двух сценариях:

1. **Восстановление после переполнения**: Модель возвращает ошибку переполнения контекста → сжатие → повтор
2. **Поддержание порога**: После успешного хода, когда:
   ```
   contextTokens > contextWindow - reserveTokens
   ```

### Сжатие против обрезки сессий

Это два разных механизма:

| Функция | Сжатие | Обрезка сессий |
|---------|--------|----------------|
| **Действие** | Суммирует и сохраняет историю разговоров | Обрезает старые результаты инструментов в памяти |
| **Персистентность** | Записывает в JSONL транскрипт | Временно, только для каждого запроса |
| **Цель** | Все типы сообщений | Только результаты инструментов |
| **Триггер** | Порог контекстного окна | Истечение TTL кэша |

## Распространенные проблемы с памятью сессий

### Проблема #1: Потеря учетных данных во время сжатия

**Симптом**: После автоматического сжатия агент "забывает" API-ключи, SSH-учетные данные или состояние рабочего пространства.

**Основная причина**: Критически важный контекст, хранящийся только в недавних сообщениях, суммируется до того, как агент запишет его в постоянное хранилище.

**Решение**: Включите сброс памяти перед сжатием.

Сброс памяти запускает тихий агентский ход перед сжатием, предлагая агенту записать важную информацию на диск:

```json5
// ~/.openclaw/openclaw.json
{
  agents: {
    defaults: {
      compaction: {
        reserveTokensFloor: 20000,
        memoryFlush: {
          enabled: true,
          softThresholdTokens: 4000,
          systemPrompt: "Сессия приближается к сжатию. Сохраните постоянные воспоминания сейчас.",
          prompt: "Запишите постоянные заметки в memory/YYYY-MM-DD.md; ответьте NO_REPLY, если нечего сохранять."
        }
      }
    }
  }
}
```

**Как это работает**:
- Срабатывает когда: `contextTokens > (contextWindow - reserveTokensFloor - softThresholdTokens)`
- Выполняется один раз за цикл сжатия (отслеживается в `sessions.json`)
- Использует соглашение `NO_REPLY` для подавления видимого пользователю вывода
- Выполняется только когда рабочее пространство доступно для записи

### Проблема #2: Раздувание контекстного окна из-за результатов инструментов

**Симптом**: Сессии быстро приближаются к лимиту контекста, несмотря на мало пользовательских сообщений.

**Основная причина**: Большие выводы инструментов (например, логи `exec`, чтение файлов, веб-скрейпинг) накапливаются в транскрипте.

**Решение**: Включите обрезку сессий с TTL кэша.

```json5
{
  agents: {
    defaults: {
      contextPruning: {
        mode: "cache-ttl",
        ttl: "5m",
        keepLastAssistants: 3,
        softTrimRatio: 0.3,
        hardClearRatio: 0.5,
        minPrunableToolChars: 50000,
        softTrim: {
          maxChars: 4000,
          headChars: 1500,
          tailChars: 1500
        },
        hardClear: {
          enabled: true,
          placeholder: "[Содержимое старого результата инструмента очищено]"
        }
      }
    }
  }
}
```

**Умные значения по умолчанию для пользователей Anthropic**:
- Профили OAuth/setup-token: `cache-ttl` включен, heartbeat `1h`
- Профили API-ключа: `cache-ttl` включен, heartbeat `30m`, TTL кэша `1h`

**Что обрезается**:
- Только сообщения `toolResult` (сообщения пользователь/ассистент никогда не изменяются)
- Результаты инструментов до последних N сообщений ассистента
- Слишком большие результаты получают мягкую обрезку (голова + хвост сохраняются с `...` между ними)
- Очень старые результаты жестко очищаются текстом-заполнителем
- Блоки изображений всегда защищены

### Проблема #3: Чрезмерное срабатывание сжатия

**Симптом**: `🧹 Автоматическое сжатие завершено` появляется часто, даже в коротких сессиях.

**Основные причины**:
1. Контекстное окно модели слишком мало для рабочей нагрузки
2. `reserveTokens` установлено слишком высоко относительно контекстного окна
3. Файлы рабочего пространства, внедряемые при запуске сессии, очень большие

**Диагностика**:

```bash
# Проверить текущее состояние сессии
# В чате:
/status

# Из CLI:
openclaw status

# Проверить разбивку контекста
# В чате:
/context detail
```

**Решения**:

1. **Увеличить минимум резервных токенов** (оставляет больше запаса):
```json5
{
  agents: {
    defaults: {
      compaction: {
        reserveTokensFloor: 25000  // По умолчанию: 20000
      }
    }
  }
}
```

2. **Уменьшить внедрение файлов рабочего пространства** (ограничить размер файлов загрузки):
```json5
{
  agents: {
    defaults: {
      bootstrapMaxChars: 15000  // По умолчанию: 20000
    }
  }
}
```

3. **Стратегически использовать ручное сжатие**:
```
/compact Сосредоточиться на решениях и открытых вопросах
```

### Проблема #4: Утечка приватного контекста из-за изоляции сессий

**Симптом**: Личная информация из личных сообщений появляется в групповых чатах, или несколько пользователей видят контекст друг друга.

**Основная причина**: По умолчанию `session.dmScope = "main"` делится всеми личными сообщениями в один ключ сессии.

**Решение**: Включите безопасный режим личных сообщений для многопользовательских сценариев.

```json5
{
  session: {
    // Изолировать контекст личных сообщений по каналу + отправителю
    dmScope: "per-channel-peer",
    
    // Для многоаккаунтных почтовых ящиков:
    // dmScope: "per-account-channel-peer",
    
    // Опционально: связать одного человека через каналы
    identityLinks: {
      alice: ["telegram:123456789", "discord:987654321"]
    }
  }
}
```

**Опции области личных сообщений**:
- `main` (по умолчанию): Все личные сообщения делятся одной сессией—отлично для непрерывности одного пользователя
- `per-peer`: Изоляция по ID отправителя через каналы
- `per-channel-peer`: Изоляция по каналу + отправителю (рекомендуется для общих почтовых ящиков)
- `per-account-channel-peer`: Изоляция по аккаунту + каналу + отправителю (мультиаккаунт)

## Лучшие практики ручного управления памятью

### Соглашение NO_REPLY

При выполнении тихих операций обслуживания используйте префикс `NO_REPLY` для подавления доставки:

```
NO_REPLY

[Агент выполняет запись в память, обновление файлов и т.д.]
```

Когда вывод начинается с `NO_REPLY`, OpenClaw подавляет индикаторы черновика/набора текста, предотвращая частичные утечки во время тихих операций.

### Организация файлов памяти

OpenClaw использует обычный Markdown для памяти:

```
~/.openclaw/workspace/
├── MEMORY.md              # Долгосрочная, кураторская память (только основная сессия)
├── memory/
│   ├── 2026-02-10.md     # Дневные логи (только добавление)
│   ├── 2026-02-11.md
│   └── 2026-02-13.md
└── AGENTS.md, SOUL.md, USER.md и т.д.
```

**Лучшие практики**:
- **Решения и предпочтения** → `MEMORY.md`
- **Дневной контекст и заметки** → `memory/YYYY-MM-DD.md`
- **Никогда не полагайтесь на "мысленные заметки"** → Записывайте все важное на диск
- **MEMORY.md загружается только в основной сессии** → Никогда в групповых контекстах (безопасность)

### Настройка поиска по памяти

OpenClaw может построить векторный индекс для файлов памяти для семантического поиска:

```json5
{
  agents: {
    defaults: {
      memorySearch: {
        enabled: true,
        provider: "openai",  // или "gemini", "local"
        model: "text-embedding-3-small",
        
        // Гибридный поиск (BM25 + векторы)
        query: {
          hybrid: {
            enabled: true,
            vectorWeight: 0.7,
            textWeight: 0.3,
            candidateMultiplier: 4
          }
        },
        
        // Индексировать дополнительные каталоги
        extraPaths: ["../team-docs", "/srv/shared-notes/overview.md"]
      }
    }
  }
}
```

**Опции провайдеров**:
- `openai`: Использует API эмбеддингов OpenAI (поддерживает пакетную индексацию для экономии затрат)
- `gemini`: Использует эмбеддинги Google Gemini
- `local`: Использует node-llama-cpp с моделями GGUF (автоматическая загрузка ~300MB)

**Преимущества гибридного поиска**:
- Векторы: Семантическое соответствие ("Mac Studio gateway host" ≈ "машина, запускающая gateway")
- BM25: Точные токены (ID, коды ошибок, символы кода)
- Комбинация: Лучшее из обоих миров

## Отладка затрат на токены и использования контекста

### Просмотр использования токенов в чате

```bash
# Показать текущее состояние сессии
/status

# Включить футер использования для каждого ответа
/usage tokens     # Показывает счетчики токенов
/usage full       # Показывает токены + расчетную стоимость (только API-ключ)
/usage cost       # Показывает локальную сводку затрат из логов

# Отключить футер использования
/usage off

# Просмотр разбивки контекста
/context list     # Высокоуровневая разбивка
/context detail   # Детальные размеры по файлам
```

### Понимание оценки затрат

OpenClaw оценивает затраты из конфигурации ценообразования модели:

```json5
{
  models: {
    providers: {
      anthropic: {
        models: [
          {
            id: "claude-sonnet-4-5",
            cost: {
              input: 3.0,      // USD за 1M токенов
              output: 15.0,
              cacheRead: 0.3,
              cacheWrite: 3.75
            }
          }
        ]
      }
    }
  }
}
```

**Поведение кэша с обрезкой**:
- Кэширование промптов Anthropic применяется только в окне TTL
- Обрезка с TTL кэша снижает затраты на запись в кэш при истечении кэша
- Heartbeat может поддерживать кэш теплым (установите интервал чуть ниже TTL кэша)

**Пример: Поддержание теплого 1h кэша**:
```json5
{
  agents: {
    defaults: {
      model: {
        primary: "anthropic/claude-opus-4-5"
      },
      models: {
        "anthropic/claude-opus-4-5": {
          params: {
            cacheRetention: "long"
          }
        }
      },
      heartbeat: {
        every: "55m"  // Чуть ниже 1h TTL кэша
      }
    }
  }
}
```

## Продвинутое: Долгосрочная память с LanceDB

Для производственных развертываний с обширными требованиями к памяти OpenClaw поддерживает LanceDB в качестве альтернативного бэкенда памяти:

```json5
{
  plugins: {
    slots: {
      memory: "memory-lancedb"  // Вместо "memory-core" по умолчанию
    }
  }
}
```

**Преимущества LanceDB**:
- Персистентное векторное хранилище, оптимизированное для больших наборов данных
- Лучшая производительность в масштабе (тысячи записей памяти)
- Автоматические рабочие процессы отзыва и захвата
- Продвинутая фильтрация и запросы метаданных

**Предварительные требования**:
- Отдельный сервер/экземпляр LanceDB
- Дополнительная конфигурация для деталей подключения

## Продвинутое: Бэкенд памяти QMD (экспериментальный)

QMD объединяет BM25 + векторы + переранжирование в локальном поисковом боковом сервисе:

```json5
{
  memory: {
    backend: "qmd",
    qmd: {
      includeDefaultMemory: true,
      update: { interval: "5m", debounceMs: 15000 },
      limits: { maxResults: 6, timeoutMs: 4000 },
      scope: {
        default: "deny",
        rules: [{ action: "allow", match: { chatType: "direct" } }]
      },
      paths: [
        { name: "docs", path: "~/notes", pattern: "**/*.md" }
      ]
    }
  }
}
```

**Предварительные требования**:
- Установить QMD CLI: `bun install -g github.com/tobi/qmd`
- SQLite с поддержкой расширений: `brew install sqlite` (macOS)
- Первый поиск автоматически загружает модели GGUF (~300MB)

**Функции QMD**:
- Локальный подход (без внешнего API)
- Гибридный поиск (BM25 + векторы + переранжирование)
- Индексация транскриптов сессий (opt-in)
- Поддержка пользовательских коллекций

## Контрольный список устранения неполадок

### 1. Проблемы с ключами сессий

```bash
# Проверить текущий ключ сессии
/status

# Проверить, что маршрутизация сессий соответствует ожиданиям
openclaw sessions --json

# Подтвердить конфигурацию dmScope
cat ~/.openclaw/openclaw.json | grep -A5 dmScope
```

### 2. Проблемы с контекстным окном

```bash
# Просмотр текущего использования контекста
/status

# Детальная разбивка
/context detail

# Проверить контекстное окно модели
openclaw models list | grep <model-name>

# Запустить ручное сжатие
/compact Включить недавние решения и открытые задачи
```

### 3. Память не сохраняется

```bash
# Проверить существование файлов памяти
ls -la ~/.openclaw/workspace/memory/

# Проверить конфигурацию сброса памяти
cat ~/.openclaw/openclaw.json | grep -A10 memoryFlush

# Проверить доступ к рабочему пространству (должен быть доступен для записи)
openclaw status | grep workspace
```

### 4. Чрезмерные затраты на токены

```bash
# Включить обрезку сессий
# Редактировать ~/.openclaw/openclaw.json:
{
  "agents": {
    "defaults": {
      "contextPruning": {
        "mode": "cache-ttl",
        "ttl": "5m"
      }
    }
  }
}

# Перезапустить gateway
openclaw gateway restart

# Мониторить снижение затрат
/usage cost
```

### 5. Утечка тихих операций

Убедитесь, что операции начинаются с точного токена `NO_REPLY`:

```
NO_REPLY

Выполнение операции записи в память...
```

Требуется сборка OpenClaw `2026.1.10` или новее для подавления потоковой передачи.

## Заключение

Системы памяти сессий и управления контекстом OpenClaw мощны, но требуют понимания для оптимальной настройки. Ключевые выводы:

1. **Включите сброс памяти перед сжатием** для предотвращения потери учетных данных
2. **Используйте обрезку сессий с TTL кэша** для контроля раздувания результатов инструментов
3. **Настройте безопасный режим личных сообщений** для многопользовательских сценариев
4. **Мониторьте затраты на токены** с командами `/usage` и `/context`
5. **Записывайте важную информацию на диск** (никогда не полагайтесь на состояние в памяти)
6. **Выберите подходящий бэкенд памяти** (core, LanceDB или QMD) для вашего масштаба

С этими конфигурациями и методами устранения неполадок вы можете поддерживать здоровые сессии с надежной персистентностью памяти и оптимальным использованием токенов.

## Дополнительное чтение

- [Документация по сессиям OpenClaw](https://docs.openclaw.ai/concepts/session)
- [Глубокое погружение в сжатие](https://docs.openclaw.ai/concepts/compaction)
- [Архитектура памяти](https://docs.openclaw.ai/concepts/memory)
- [Использование и затраты на токены](https://docs.openclaw.ai/token-use)
- [Обрезка сессий](https://docs.openclaw.ai/concepts/session-pruning)
