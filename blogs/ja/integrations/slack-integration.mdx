---
title: OpenClaw Slack 連携 - ボット、チャンネル、エンタープライズ設定完全ガイド
description: OpenClaw と Slack を接続する完全ガイド。Socket Mode、HTTP webhooks、Bot Token、DM ポリシー、チャンネル許可リスト、スレッド、スラッシュコマンド、マルチワークスペース設定を網羅。
slug: /slack-integration
publishedAt: 2025-02-15
status: published
visibility: public
featuredImageUrl: /images/features/slack-integration.webp
---

# OpenClaw Slack 連携：完全設定ガイド

AI アシスタントを Slack に導入し、チャンネル、DM、スレッドでコラボレーションしましょう。OpenClaw は Slack のワークスペースモデルと深く統合されています — 簡単セットアップの Socket Mode、本番環境向けの HTTP webhooks、エンタープライズ環境向けの細かいアクセス制御をサポート。

このガイドでは、Slack アプリの作成からチャンネル権限、スレッド動作、スラッシュコマンド、マルチワークスペース展開まですべてをカバーします。

## 仕組み

OpenClaw は Slack API を通じて接続し、2つのモードをサポート：

- **Socket Mode**（推奨）：リアルタイム WebSocket 接続、パブリック URL 不要
- **HTTP Mode**：Events API webhooks、本番展開向け

両モードとも DM、チャンネル、スレッド、リアクション、ファイルアップロード、スラッシュコマンドをサポート。

## 前提条件

始める前に：

- OpenClaw Gateway がインストールされ実行中
- 管理者アクセス権のある Slack ワークスペース
- Slack のチャンネルと DM モデルの理解

## クイックスタート（Socket Mode）

### ステップ 1：Slack アプリを作成

1. [api.slack.com/apps](https://api.slack.com/apps) にアクセス
2. **Create New App** → **From scratch** をクリック
3. "OpenClaw" と命名してワークスペースを選択

### ステップ 2：Socket Mode を有効化

1. **Settings → Socket Mode** に移動
2. **Enable Socket Mode** をオン
3. **Basic Information → App-Level Tokens** に移動
4. **Generate Token and Scopes** をクリック
5. scope `connections:write` を追加
6. **App Token** (`xapp-...`) をコピー

### ステップ 3：OAuth 権限を設定

1. **OAuth & Permissions** に移動
2. これらの **Bot Token Scopes** を追加：
   - `chat:write` — メッセージ送信
   - `channels:history`, `channels:read` — チャンネル読み取り
   - `im:history`, `im:read`, `im:write` — ダイレクトメッセージ
   - `users:read` — ユーザー情報
   - `reactions:read`, `reactions:write` — リアクション
   - `app_mentions:read` — @メンション
   - `commands` — スラッシュコマンド
3. **Install to Workspace** をクリック
4. **Bot User OAuth Token** (`xoxb-...`) をコピー

### ステップ 4：イベントを購読

1. **Event Subscriptions** に移動
2. **Enable Events** をオン
3. **Subscribe to bot events** に追加：
   - `message.channels`, `message.groups`, `message.im`, `message.mpim`
   - `app_mention`
   - `reaction_added`, `reaction_removed`

### ステップ 5：OpenClaw を設定

環境変数でトークンを設定：

```bash
export SLACK_APP_TOKEN="xapp-..."
export SLACK_BOT_TOKEN="xoxb-..."
```

または `~/.openclaw/openclaw.json` で：

```json5
{
  channels: {
    slack: {
      enabled: true,
      appToken: "xapp-...",
      botToken: "xoxb-...",
    },
  },
}
```

### ステップ 6：ボットを招待

チャンネルに OpenClaw を招待：

```
/invite @OpenClaw
```

## DM アクセス制御

### ペアリングモード（デフォルト、安全）

未知の送信者にはペアリングコードが送られます：

```json5
{
  channels: {
    slack: {
      dm: {
        policy: "pairing",
      },
    },
  },
}
```

**送信者を承認：**

```bash
openclaw pairing list slack
openclaw pairing approve slack <CODE>
```

### 許可リストモード

特定のユーザーのみ DM 可能：

```json5
{
  channels: {
    slack: {
      dm: {
        policy: "allowlist",
        allowFrom: ["U123ABC", "@jane"],
      },
    },
  },
}
```

## チャンネル設定

### グループポリシー

ボットがアクセスできるチャンネルを制御：

```json5
{
  channels: {
    slack: {
      groupPolicy: "allowlist",
      channels: {
        "#general": { allow: true, requireMention: true },
        "#support": { allow: true, requireMention: false },
      },
    },
  },
}
```

## 返信スレッド

ボットのスレッド返信を制御：

```json5
{
  channels: {
    slack: {
      replyToMode: "off", // "off" | "first" | "all"
    },
  },
}
```

| モード | 動作 |
|--------|------|
| `off` | メインチャンネルで返信（デフォルト） |
| `first` | 最初の返信はスレッド、以降はチャンネル |
| `all` | すべての返信をスレッドに |

## スラッシュコマンド

```json5
{
  channels: {
    slack: {
      slashCommand: {
        enabled: true,
        name: "openclaw",
        ephemeral: true,
      },
    },
  },
}
```

## 完全な設定例

```json5
{
  channels: {
    slack: {
      enabled: true,
      appToken: "xapp-...",
      botToken: "xoxb-...",
      
      dm: {
        policy: "pairing",
        allowFrom: ["U123ABC"],
      },
      
      groupPolicy: "allowlist",
      channels: {
        "#general": { allow: true, requireMention: true },
      },
      
      replyToMode: "first",
      historyLimit: 50,
      
      actions: {
        reactions: true,
        messages: true,
      },
    },
  },
}
```

## CLI コマンド

```bash
# ステータス確認
openclaw channels status --probe

# メッセージ送信
openclaw message send --channel slack --to "user:U123" --message "こんにちは！"

# ログ表示
openclaw channels logs --channel slack
```

## トラブルシューティング

### ボットが応答しない

1. トークンが正しいか確認
2. Socket Mode が有効か確認
3. ボットがチャンネルに招待されているか確認

### "missing scope" エラー

**OAuth & Permissions** で必要なスコープを追加してアプリを再インストール。

## まとめ

Slack 連携を設定しました：

- ✅ **Socket Mode** または HTTP webhooks
- ✅ **DM ポリシー** アクセス制御用
- ✅ **チャンネル許可リスト** メンション制限付き
- ✅ **スレッド** 設定
- ✅ **スラッシュコマンド** サポート

AI アシスタントが Slack で利用可能になりました！💼
