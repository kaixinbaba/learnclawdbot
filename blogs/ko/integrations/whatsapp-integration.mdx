---
title: OpenClaw WhatsApp 연동 - 그룹, 미디어, 멀티 계정 완전 가이드
description: Baileys를 사용하여 OpenClaw를 WhatsApp에 연결하는 완전 가이드. QR 로그인, DM 정책, 그룹 채팅, 미디어 처리, 페어링 모드, 멀티 계정 설정을 다룹니다.
slug: /whatsapp-integration
tags: whatsapp, 연동, baileys, 메시징, 그룹, 미디어
publishedAt: 2025-02-15
status: published
visibility: public
featuredImageUrl: /images/features/whatsapp-integration.webp
---

# OpenClaw WhatsApp 연동: 완전 설정 가이드

AI 어시스턴트를 WhatsApp에 연결하여 친구와 대화하듯이 채팅하세요. OpenClaw는 WhatsApp Web(Baileys 사용)을 통해 원활한 메시징 경험을 제공합니다 — 비즈니스 API 불필요, 24시간 응답 창 제한 없음, 순수한 개인 어시스턴트 매직입니다.

이 가이드는 기본 QR 코드 로그인부터 고급 그룹 채팅 설정, 미디어 처리, 멀티 계정 설정까지 모든 것을 다룹니다.

## 작동 방식

OpenClaw는 Baileys 라이브러리를 사용하여 WhatsApp Web에 연결합니다(많은 WhatsApp 봇 뒤에 있는 동일한 기술). 휴대폰이 인증 장치 역할을 하고 OpenClaw는 컴퓨터에서 지속적인 세션을 유지합니다.

**WhatsApp Business API 대비 장점:**
- 24시간 응답 창 제한 없음
- 개인 번호로 작동
- Meta Business Manager 설정 불필요
- 완전한 그룹 채팅 지원

## 사전 요구 사항

시작하기 전에:

- OpenClaw Gateway 설치 및 실행 중
- WhatsApp용 전화번호(개인 번호 또는 전용 번호)
- Node.js 런타임(**Bun 불가** — Baileys는 Bun에서 불안정)

## 1단계: 기본 설정

WhatsApp을 `~/.openclaw/openclaw.json`에 추가:

```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "allowlist",
      allowFrom: ["+821012345678"], // 귀하의 전화번호(E.164 형식)
    },
  },
}
```

**중요:** E.164 형식의 전화번호 사용(예: `+82`는 한국, `+1`은 미국).

## 2단계: QR 코드로 로그인

WhatsApp 계정 연결:

```bash
openclaw channels login --channel whatsapp
```

터미널에 QR 코드가 표시됩니다. 휴대폰으로 스캔:

1. 휴대폰에서 WhatsApp 열기
2. **설정 → 연결된 기기**로 이동
3. **기기 연결** 탭
4. QR 코드 스캔

연결되면 확인 메시지가 표시됩니다. 세션은 `~/.openclaw/credentials/whatsapp/default/creds.json`에 저장됩니다.

## 3단계: 연결 확인

채널 상태 확인:

```bash
openclaw channels status --probe
```

WhatsApp이 `linked: true`와 `running`으로 표시되어야 합니다.

## 전화번호 옵션

### 옵션 A: 전용 번호(권장)

OpenClaw용으로 **별도의 전화번호** 사용. 장점:
- 개인 메시지와 명확한 분리
- 연락처 혼동 위험 없음
- 트러블슈팅 용이

**최적 설정:** 구형 Android 폰 + eSIM을 Wi-Fi에 연결하고 WhatsApp Business 실행.

### 옵션 B: 개인 번호(대안)

**자신의 WhatsApp 번호**에서 OpenClaw 실행. "나에게 메시지"로 테스트. 셀프 채팅 모드 활성화:

```json5
{
  channels: {
    whatsapp: {
      selfChatMode: true,
      dmPolicy: "allowlist",
      allowFrom: ["+821012345678"],
    },
  },
}
```

## DM 접근 제어

### 허용 목록 모드(기본값, 가장 안전)

특정 번호만 봇에 메시지 가능:

```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "allowlist",
      allowFrom: ["+821012345678", "+821087654321"],
    },
  },
}
```

### 페어링 모드(새 발신자 승인)

알 수 없는 발신자에게 페어링 코드 전송. 수동으로 승인:

```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "pairing",
    },
  },
}
```

**발신자 승인:**

```bash
openclaw pairing list whatsapp
openclaw pairing approve whatsapp <CODE>
```

### 오픈 모드(모두 허용)

누구나 봇에 메시지 가능(**주의해서 사용**):

```json5
{
  channels: {
    whatsapp: {
      dmPolicy: "open",
      allowFrom: ["*"],
    },
  },
}
```

## 그룹 채팅 설정

OpenClaw는 스마트한 활성화로 WhatsApp 그룹에 참여 가능.

### 그룹 접근 활성화

```json5
{
  channels: {
    whatsapp: {
      groupPolicy: "allowlist",
      groups: {
        "*": { requireMention: true }, // 모든 그룹 허용, @멘션 필요
      },
    },
  },
}
```

### 활성화 모드

**멘션 모드(기본값):** 봇은 @멘션될 때만 응답:

```json5
{
  agents: {
    list: [
      {
        id: "main",
        groupChat: {
          mentionPatterns: ["@openclaw", "openclaw"],
          historyLimit: 50,
        },
      },
    ],
  },
}
```

**항상 모드:** 봇이 모든 메시지에 응답(신중하게 사용):

```
/activation always
```

`/activation mention`으로 전환(소유자 전용 명령).

## 미디어 처리

### 미디어 수신

OpenClaw는 수신 미디어를 다운로드하고 처리:

```json5
{
  channels: {
    whatsapp: {
      mediaMaxMb: 50,
    },
  },
}
```

### 미디어 전송

봇은 이미지, 비디오, 오디오, 문서 전송 가능:

```json5
{
  agents: {
    defaults: {
      mediaMaxMb: 5,
    },
  },
}
```

## 자동 반응

메시지 수신 시 이모지 반응 전송:

```json5
{
  channels: {
    whatsapp: {
      ackReaction: {
        emoji: "👀",
        direct: true,
        group: "mentions",
      },
    },
  },
}
```

## 멀티 계정 설정

하나의 Gateway에서 여러 WhatsApp 계정 실행:

```json5
{
  channels: {
    whatsapp: {
      accounts: {
        personal: {
          allowFrom: ["+821011111111"],
        },
        work: {
          allowFrom: ["+821022222222"],
          ackReaction: { emoji: "✅" },
        },
      },
    },
  },
}
```

**특정 계정으로 로그인:**

```bash
openclaw channels login --account personal
openclaw channels login --account work
```

## CLI 명령어

```bash
# 상태 확인
openclaw channels status --probe

# 메시지 전송
openclaw message send --channel whatsapp --to "+821012345678" --message "안녕하세요!"

# 로그 보기
openclaw channels logs --channel whatsapp

# 로그아웃
openclaw channels logout --channel whatsapp
```

## 문제 해결

### "연결되지 않음" 또는 QR 로그인 필요

**증상:** `channels status`가 `linked: false` 표시

**해결:** `openclaw channels login` 실행하고 QR 코드 다시 스캔.

### 연결되었지만 끊김 / 재연결 루프

**해결:**
1. `openclaw doctor` 실행
2. Gateway 재시작
3. 지속되면 `openclaw channels login`으로 재연결

### Bun 런타임 문제

**해결:** WhatsApp(Baileys)는 Bun에서 불안정. Node.js로 전환:

```bash
export OPENCLAW_RUNTIME=node
```

## 요약

WhatsApp 연동을 설정했습니다:

- ✅ **QR 코드 로그인** WhatsApp Web 통해
- ✅ **DM 정책** 접근 제어용
- ✅ **그룹 채팅 지원** 멘션 활성화 포함
- ✅ **미디어 처리** 이미지, 비디오, 음성 메시지 지원
- ✅ **자동 반응** 즉각적인 피드백
- ✅ **멀티 계정** 지원

AI 어시스턴트가 이제 WhatsApp에서 사용 가능합니다! 💬
