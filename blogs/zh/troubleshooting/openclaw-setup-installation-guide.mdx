---
title: "完整指南：在 Windows、macOS 和 Linux 上安装 OpenClaw"
description: "跨平台安装 OpenClaw 的详细教程。解决常见的 Node.js、npm 和依赖问题"
slug: /troubleshooting/openclaw-setup-installation-guide
tags: installation, setup, troubleshooting, node.js, cross-platform
publishedAt: 2026-02-12
status: published
visibility: public
isPinned: false
featuredImageUrl: /images/features/openclaw-setup-guide.webp
---

# 完整指南：在 Windows、macOS 和 Linux 上安装 OpenClaw

在你的系统上运行 OpenClaw 本应很简单,但不同操作系统有各自的独特问题。本指南将逐步指导你在 **Windows**、**macOS** 和 **Linux** 上安装,并解决常见问题。

## 前置要求：你需要准备什么

安装 OpenClaw 之前,确保你已经具备:

### 1. Node.js (v18 或更高版本)

OpenClaw 需要现代 Node.js 运行时。检查你的版本:

```bash
node --version
```

如果没有 Node.js 或版本过旧,请安装:

**macOS (使用 Homebrew):**
```bash
brew install node
```

**Windows (使用 winget):**
```powershell
winget install OpenJS.NodeJS
```

**Linux (Ubuntu/Debian):**
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**Linux (Fedora/RHEL):**
```bash
sudo dnf install nodejs
```

### 2. npm 或 pnpm

npm 随 Node.js 一起安装,但 pnpm 更快更高效:

```bash
npm install -g pnpm
```

## 安装 OpenClaw

### 步骤 1: 通过 npm 安装

```bash
npm install -g @anthropic-ai/openclaw
```

或使用 pnpm:

```bash
pnpm add -g @anthropic-ai/openclaw
```

### 步骤 2: 验证安装

```bash
openclaw --version
```

你应该看到版本号(例如 `0.5.2`)。

## 平台特定设置

### 🪟 Windows

#### 问题 #1: PowerShell 执行策略

**症状:** `openclaw: 无法加载文件...因为在此系统上禁止运行脚本`

**解决方案:**
```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

#### 问题 #2: 长路径支持

Windows 有 260 字符路径长度限制,可能导致 npm 安装失败。

**解决方案:** 在注册表中启用长路径
```powershell
New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
```

或使用 WSL2 (Windows 子系统 for Linux):
```powershell
wsl --install
```

然后在 WSL 内安装 OpenClaw:
```bash
# 在 WSL 中
npm install -g @anthropic-ai/openclaw
```

#### 问题 #3: Node-gyp 编译错误

**症状:** 安装时出现缺少构建工具的错误

**解决方案:** 安装 Windows 构建工具
```powershell
npm install -g windows-build-tools
```

或从 Microsoft 手动安装 Visual Studio 构建工具。

### 🍎 macOS

#### 问题 #1: 权限被拒绝

**症状:** 运行 `npm install -g` 时出现 `EACCES: permission denied`

**解决方案:** 永远不要在 npm 中使用 `sudo`。而是配置 npm 使用用户目录:

```bash
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.zshrc
source ~/.zshrc
```

然后重新安装:
```bash
npm install -g @anthropic-ai/openclaw
```

#### 问题 #2: 安装后找不到命令

**症状:** 成功安装后仍显示 `openclaw: command not found`

**解决方案:** 检查 npm 全局 bin 是否在 PATH 中:
```bash
echo $PATH | grep npm
```

如果缺失,添加到 `~/.zshrc`:
```bash
export PATH="$(npm config get prefix)/bin:$PATH"
```

#### 问题 #3: 需要 Rosetta (M1/M2 Mac)

某些依赖可能在 Apple Silicon 上需要 Rosetta 2:

```bash
softwareupdate --install-rosetta
```

### 🐧 Linux

#### 问题 #1: 全局安装时出现 EACCES

**症状:** 与 macOS 相同的权限错误

**解决方案:** 相同方法 - 使用用户目录:
```bash
mkdir -p ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
npm install -g @anthropic-ai/openclaw
```

#### 问题 #2: 缺少依赖 (Ubuntu/Debian)

**症状:** 原生模块的构建错误

**解决方案:**
```bash
sudo apt-get update
sudo apt-get install -y build-essential python3
```

#### 问题 #3: Snap Node.js 问题

如果你通过 Snap 安装了 Node.js,可能会遇到权限问题。

**解决方案:** 卸载 Snap 版本,使用官方源:
```bash
sudo snap remove node
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

## 首次配置

安装后,配置 OpenClaw:

### 1. 设置 API 密钥

```bash
openclaw configure
```

按照提示添加你的 Anthropic API 密钥。

### 2. 启动 Gateway

```bash
openclaw gateway start
```

### 3. 测试设置

```bash
openclaw chat "你好,OpenClaw!"
```

如果看到回复,恭喜! 🎉

## 常见安装后问题

### Gateway 无法启动

**检查日志:**
```bash
tail -f ~/.openclaw/logs/gateway.log
```

**常见解决方案:**
- 端口 3000 已被占用 → 在配置中更改端口
- 配置文件缺失 → 重新运行 `openclaw configure`
- 防火墙拦截 → 在防火墙设置中允许 Node.js

### 版本过旧

更新 OpenClaw:
```bash
npm update -g @anthropic-ai/openclaw
```

或使用 pnpm:
```bash
pnpm update -g @anthropic-ai/openclaw
```

### 完全重新安装

如果一切都失败:
```bash
# 卸载
npm uninstall -g @anthropic-ai/openclaw

# 清除缓存
npm cache clean --force

# 重新安装
npm install -g @anthropic-ai/openclaw
```

## 平台对比

| 特性 | Windows | macOS | Linux |
|------|---------|-------|-------|
| 原生终端 | ❌ 使用 WSL2 | ✅ 优秀 | ✅ 优秀 |
| 安装速度 | 🐢 较慢 | 🚀 快速 | 🚀 快速 |
| 需要构建工具 | ⚠️ 经常 | 🟢 很少 | ⚠️ 有时 |
| 推荐设置 | WSL2 + Ubuntu | 原生 | 原生 |

## 下一步

安装完成后,探索:
- **[配置提供商](/providers/anthropic)** - 设置 Claude、OpenAI、Google
- **[Heartbeat 设置](/troubleshooting/fix-cron-heartbeat-not-working)** - 启用计划任务
- **[终端基础](/how-to-open-terminal)** - 学习终端命令

## 仍然有问题?

如果遇到困难:
1. 检查 [GitHub Issues](https://github.com/anthropics/claude-quickstarts/issues)
2. 查看 [官方文档](https://docs.openclaw.ai)
3. 加入社区 Discord 获取实时帮助

---

**专业提示:** 在 Windows 上,WSL2 提供最佳的 OpenClaw 体验。就像在 Windows 中运行 Linux,拥有完整的终端能力。
