---
title: "如何将 LINE 消息集成到您的 AI 聊天机器人"
description: "将 OpenClaw AI 代理连接到 LINE Messaging API 的分步指南。通过 webhook 设置、身份验证和富消息支持，为您的 LINE 用户启用智能聊天自动化。"
slug: /line-integration
publishedAt: 2026-02-16
status: published
visibility: public
featuredImageUrl: /images/features/line-integration.webp
---

# 如何将 LINE 消息集成到您的 AI 聊天机器人

LINE 是亚洲最受欢迎的消息平台之一,在日本、台湾、泰国和印度尼西亚拥有超过 2 亿月活跃用户。将您的 AI 聊天机器人与 LINE 集成,可以让您通过智能、自动化的对话接触到这一庞大的受众群体。

在本指南中,您将学习如何将 OpenClaw AI 代理连接到 LINE Messaging API,使您的聊天机器人能够处理私信、群聊、媒体分享和丰富的交互式消息。

## 您将获得什么

完成此集成后,您的 AI 助手将能够:

- **实时响应 LINE 用户**,进行智能对话
- **无缝处理群聊**和私信
- **分享媒体**,包括图片、视频和位置
- **发送富消息**,使用 LINE 的 Flex Message 和模板格式
- **提供快速回复**,改善用户体验
- **支持多个 LINE 账号**,从单个 OpenClaw 实例管理

## 前提条件

开始之前,请确保您具备:

1. **已安装并运行 OpenClaw** - 如果您还未安装 OpenClaw,请遵循[安装指南](/docs/getting-started)。
2. **LINE Developers 账号** - 在 [LINE Developers Console](https://developers.line.biz/console/) 注册。
3. **HTTPS 端点** - LINE webhook 需要 HTTPS。使用反向代理(nginx、Caddy)或隧道服务(ngrok、Cloudflare Tunnel)。
4. **Gateway 访问权限** - 确保您的 OpenClaw gateway 可以从互联网访问。

## 步骤 1:安装 LINE 插件

OpenClaw 使用插件架构管理消息通道。安装 LINE 插件:

```bash
openclaw plugins install @openclaw/line
```

如果您从 git 仓库运行 OpenClaw,可以安装本地版本:

```bash
openclaw plugins install ./extensions/line
```

## 步骤 2:创建 LINE Messaging API 频道

1. **登录 LINE Developers Console**  
   访问 [https://developers.line.biz/console/](https://developers.line.biz/console/) 并使用您的 LINE 账号登录。

2. **创建 Provider**  
   如果您还没有 Provider,请创建一个。Provider 代表您的公司或服务。

3. **添加 Messaging API 频道**  
   - 点击**创建新频道**
   - 选择 **Messaging API** 作为频道类型
   - 填写必填信息:
     - 频道名称(例如 "我的 AI 助手")
     - 频道描述
     - 类别和子类别
     - 电子邮件地址
   - 同意条款并点击**创建**

4. **获取凭证**  
   创建频道后,导航到**基本设置**和 **Messaging API** 选项卡以找到:
   - **Channel access token** - 点击 "Issue" 生成长期令牌
   - **Channel secret** - 在基本设置选项卡中找到

   请妥善保管这些凭证 - 您需要它们来配置 OpenClaw。

5. **启用 Webhooks**  
   在 **Messaging API** 选项卡中:
   - 将**使用 webhook** 切换为开启
   - 设置 webhook URL(我们将在下一步配置)
   - 如果您希望 AI 处理所有交互,请禁用**自动回复消息**和**问候消息**

## 步骤 3:配置您的 Webhook URL

LINE 需要将传入的消息发送到您的 OpenClaw gateway。默认的 webhook 路径是:

```
https://your-gateway-domain.com/line/webhook
```

将 `your-gateway-domain.com` 替换为您的实际 gateway 域名或 IP 地址。

**重要:** LINE 需要 HTTPS。如果您在本地测试,请使用隧道服务:

```bash
# 使用 ngrok
ngrok http 3100

# 使用 Cloudflare Tunnel
cloudflared tunnel --url http://localhost:3100
```

使用您的 HTTPS 端点更新 LINE Developers Console 中的 webhook URL。

## 步骤 4:配置 OpenClaw

将 LINE 频道配置添加到您的 OpenClaw 配置文件中(通常是 `~/.openclaw/config.json5` 或工作区中的 `config.json5`):

### 最小配置

```json5
{
  channels: {
    line: {
      enabled: true,
      channelAccessToken: "YOUR_CHANNEL_ACCESS_TOKEN",
      channelSecret: "YOUR_CHANNEL_SECRET",
      dmPolicy: "pairing",
    },
  },
}
```

### 使用环境变量

为了更好的安全性,使用环境变量:

```bash
export LINE_CHANNEL_ACCESS_TOKEN="your_channel_access_token_here"
export LINE_CHANNEL_SECRET="your_channel_secret_here"
```

然后在配置中引用它们:

```json5
{
  channels: {
    line: {
      enabled: true,
      // Token 和 secret 将从环境变量读取
      dmPolicy: "pairing",
    },
  },
}
```

### 使用令牌文件

对于生产部署,将凭证存储在文件中:

```json5
{
  channels: {
    line: {
      enabled: true,
      tokenFile: "/secure/path/line-token.txt",
      secretFile: "/secure/path/line-secret.txt",
      dmPolicy: "pairing",
    },
  },
}
```

### 多个 LINE 账号

您可以从一个 OpenClaw 实例管理多个 LINE 频道:

```json5
{
  channels: {
    line: {
      accounts: {
        marketing: {
          channelAccessToken: "MARKETING_TOKEN",
          channelSecret: "MARKETING_SECRET",
          webhookPath: "/line/marketing",
        },
        support: {
          channelAccessToken: "SUPPORT_TOKEN",
          channelSecret: "SUPPORT_SECRET",
          webhookPath: "/line/support",
        },
      },
    },
  },
}
```

不要忘记更新 LINE 控制台中的 webhook URL 以匹配您的自定义路径!

## 步骤 5:配置访问控制

LINE 集成支持多种访问策略来控制谁可以与您的机器人交互:

### 配对模式(推荐)

这是默认且最安全的选项。新用户必须获得批准才能聊天:

```json5
{
  channels: {
    line: {
      dmPolicy: "pairing",
    },
  },
}
```

当新用户给您的机器人发消息时,他们将收到配对代码。使用以下命令批准他们:

```bash
# 列出待处理的配对请求
openclaw pairing list line

# 批准用户
openclaw pairing approve line <CODE>
```

### 白名单模式

只有指定的 LINE 用户 ID 可以与您的机器人交互:

```json5
{
  channels: {
    line: {
      dmPolicy: "allowlist",
      allowFrom: [
        "U1234567890abcdef1234567890abcdef",
        "Uabcdef1234567890abcdef1234567890",
      ],
    },
  },
}
```

### 开放模式

任何人都可以给您的机器人发消息(谨慎使用):

```json5
{
  channels: {
    line: {
      dmPolicy: "open",
    },
  },
}
```

### 群聊策略

单独配置 LINE 群聊的访问权限:

```json5
{
  channels: {
    line: {
      groupPolicy: "allowlist",
      groupAllowFrom: [
        "U1234567890abcdef1234567890abcdef", // 可以将机器人添加到群组的用户
      ],
      groups: {
        "C1234567890abcdef1234567890abcdef": {
          // 每个群组的覆盖设置
          allowFrom: ["Uabcdef1234567890abcdef1234567890"],
        },
      },
    },
  },
}
```

**注意:** LINE ID 区分大小写,并遵循以下模式:
- 用户 ID: `U` + 32 个十六进制字符
- 群组 ID: `C` + 32 个十六进制字符
- 房间 ID: `R` + 32 个十六进制字符

## 步骤 6:重启并测试

重启您的 OpenClaw gateway 以应用配置:

```bash
openclaw gateway restart
```

现在测试集成:

1. **将您的机器人添加为好友** - 扫描 LINE Developers Console 中的二维码或通过 LINE ID 搜索
2. **发送测试消息** - 向您的机器人输入 "你好" 或任何消息
3. **检查响应** - 如果启用了配对,您将首先收到配对代码

监控 gateway 日志以查看 webhook 事件:

```bash
openclaw gateway logs -f
```

您应该看到传入的 LINE webhook 事件和传出的响应。

## 高级功能

### 使用 Flex Cards 发送富消息

LINE 支持称为 Flex Messages 的强大富消息。使用 `channelData` 发送交互式卡片:

```json5
{
  text: "这是您的订单状态",
  channelData: {
    line: {
      flexMessage: {
        altText: "订单状态卡片",
        contents: {
          type: "bubble",
          body: {
            type: "box",
            layout: "vertical",
            contents: [
              {
                type: "text",
                text: "订单 #12345",
                weight: "bold",
                size: "xl",
              },
              {
                type: "text",
                text: "状态: 已发货",
                color: "#00B900",
              },
            ],
          },
        },
      },
    },
  },
}
```

### 快速回复

提供快速回复按钮以改善用户体验:

```json5
{
  text: "您想做什么?",
  channelData: {
    line: {
      quickReplies: ["查看状态", "联系支持", "查看历史"],
    },
  },
}
```

### 位置分享

发送位置信息:

```json5
{
  text: "这是我们的办公室位置",
  channelData: {
    line: {
      location: {
        title: "OpenClaw 总部",
        address: "科技城 AI 街 123 号",
        latitude: 35.681236,
        longitude: 139.767125,
      },
    },
  },
}
```

### 媒体处理

LINE 插件自动处理媒体上传和下载。配置大小限制:

```json5
{
  channels: {
    line: {
      mediaMaxMb: 20, // 默认为 10 MB
    },
  },
}
```

## 故障排除

### Webhook 验证失败

**问题:** LINE 控制台显示 "Webhook 验证失败"

**解决方案:**
- 确保您的 webhook URL 使用 HTTPS(而非 HTTP)
- 验证配置中的 `channelSecret` 与 LINE 控制台中的完全匹配
- 检查您的 gateway 是否可以从互联网访问
- 手动测试端点: `curl https://your-gateway/line/webhook`

### 没有收到消息

**问题:** 机器人没有收到来自 LINE 用户的消息

**解决方案:**
- 确认 webhook 路径与配置中的 `channels.line.webhookPath` 匹配
- 检查 gateway 日志中的 webhook POST 请求: `openclaw gateway logs -f`
- 验证 LINE Developers Console 中已启用 webhook URL
- 确保 LINE 控制台中禁用了自动回复(它会干扰 webhook 传递)

### 媒体下载错误

**问题:** 图片或视频下载失败

**解决方案:**
- 如果文件超过 10 MB,请增加 `channels.line.mediaMaxMb`
- 检查 gateway 对临时目录的写入权限
- 验证您的 LINE channel access token 具有媒体下载权限

### 无法发送消息

**问题:** 机器人收到消息但无法发送回复

**解决方案:**
- 验证 `channelAccessToken` 有效且未过期
- 检查令牌在 LINE 控制台中是否具有 "SEND_MESSAGE" 权限
- 查看 gateway 日志中的 API 错误响应
- 确保用户没有屏蔽您的机器人

### 配对批准不起作用

**问题:** 无法批准配对请求

**解决方案:**
- 运行 `openclaw pairing list line` 查看待处理的请求
- 验证配对代码正确(区分大小写)
- 检查配置中 `dmPolicy` 是否设置为 `"pairing"`
- 批准后重启 gateway: `openclaw gateway restart`

## 总结

恭喜!您已成功将 LINE Messaging 与您的 OpenClaw AI 聊天机器人集成。您的机器人现在可以:

✅ 接收和响应 LINE 消息  
✅ 处理群聊和私信  
✅ 使用 Flex Messages 发送富交互内容  
✅ 分享媒体、位置和快速回复  
✅ 通过配对、白名单或开放策略控制访问  
✅ 从一个 gateway 管理多个 LINE 账号  

### 下一步

- **自定义响应** - 配置您的代理的个性和行为
- **添加自定义命令** - 使用 `/card` 和其他工具创建 LINE 专用命令
- **监控使用情况** - 为 LINE 交互设置分析和日志记录
- **探索集成** - 将 LINE 与其他通道(如 Discord、Slack 或 WhatsApp)连接

### 有用的资源

- [LINE Messaging API 文档](https://developers.line.biz/en/docs/messaging-api/)
- [OpenClaw 频道文档](/docs/channels)
- [Flex Message 模拟器](https://developers.line.biz/flex-simulator/)
- [LINE Developers Console](https://developers.line.biz/console/)

聊天愉快! 🤖💚
