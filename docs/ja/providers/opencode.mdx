---
summary: "OpenClaw で OpenCode Zen（厳選モデル）を使用する"
read_when:
  - モデルアクセス用に OpenCode Zen が必要
  - コーディングに適した厳選モデルリストが必要
---
# OpenCode Zen

OpenCode Zen は、コーディングエージェント向けに OpenCode チームが推奨する**厳選モデルのリスト**です。これは、API キーと `opencode` プロバイダを使用するオプションのホスト型モデルアクセスパスです。Zen は現在ベータ版です。

## CLI セットアップ

```bash
openclaw onboard --auth-choice opencode-zen
# または非対話的に
openclaw onboard --opencode-zen-api-key "$OPENCODE_API_KEY"
```

## 設定スニペット

```json5
{
  env: { OPENCODE_API_KEY: "sk-..." },
  agents: { defaults: { model: { primary: "opencode/claude-opus-4-5" } } }
}
```

## 注意事項

- `OPENCODE_ZEN_API_KEY` もサポートされています。
- Zen にサインインし、請求の詳細を追加して、API キーをコピーします。
- OpenCode Zen はリクエストごとに課金します。詳細は OpenCode ダッシュボードを確認してください。
