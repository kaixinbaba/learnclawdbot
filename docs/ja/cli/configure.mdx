---
summary: "`openclaw configure`のCLIリファレンス（対話式設定プロンプト）"
read_when:
  - 認証情報、デバイス、またはエージェントのデフォルトを対話的に調整したい場合
---

# `openclaw configure`

認証情報、デバイス、エージェントのデフォルトを設定するための対話式プロンプト。

注意: **Model**セクションには、`agents.defaults.models`の許可リスト（`/model`とモデルピッカーに表示されるもの）のための複数選択が含まれるようになりました。

ヒント: サブコマンドなしの`openclaw config`は、同じウィザードを開きます。
非対話式の編集には、`openclaw config get|set|unset`を使用してください。

関連:
- Gateway設定リファレンス: [Configuration](/gateway/configuration)
- Config CLI: [Config](/cli/config)

注意事項:
- Gatewayを実行する場所を選択すると、常に`gateway.mode`が更新されます。それだけが必要な場合は、他のセクションなしで「Continue」を選択できます。
- チャネル指向のサービス（Slack/Discord/Matrix/Microsoft Teams）は、セットアップ中にチャネル/ルームの許可リストを要求します。名前またはIDを入力できます。ウィザードは可能な場合、名前をIDに解決します。

## 例

```bash
openclaw configure
openclaw configure --section models --section channels
```
