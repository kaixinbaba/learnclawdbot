---
summary: "`openclaw agent`のCLIリファレンス（Gateway経由でエージェントターンを1回送信）"
read_when:
  - スクリプトからエージェントターンを1回実行したい場合（オプションで返信を配信）
---

# `openclaw agent`

Gateway経由でエージェントターンを実行します（組み込みの場合は`--local`を使用）。
設定済みのエージェントを直接ターゲットにするには、`--agent <id>`を使用します。

関連:
- Agent sendツール: [Agent send](/tools/agent-send)

## 例

```bash
openclaw agent --to +15555550123 --message "status update" --deliver
openclaw agent --agent ops --message "Summarize logs"
openclaw agent --session-id 1234 --message "Summarize inbox" --thinking medium
openclaw agent --agent ops --message "Generate report" --deliver --reply-channel slack --reply-to "#reports"
```
