---
summary: "`openclaw tui` の CLI リファレンス（Gateway に接続されたターミナル UI）"
read_when:
  - Gateway のターミナル UI が必要な場合（リモート対応）
  - スクリプトから url/token/session を渡したい場合
---

# `openclaw tui`

Gateway に接続されたターミナル UI を開きます。

関連項目：
- TUI ガイド：[TUI](/tui)

## 例

```bash
openclaw tui
openclaw tui --url ws://127.0.0.1:18789 --token <token>
openclaw tui --session main --deliver
```
