---
summary: "`openclaw reset` の CLI リファレンス（ローカル状態/設定のリセット）"
read_when:
  - CLI をインストールしたまま、ローカル状態を消去したい場合
  - 削除される内容のドライランを実行したい場合
---

# `openclaw reset`

ローカル設定/状態をリセットします（CLI はインストールされたまま）。

```bash
openclaw reset
openclaw reset --dry-run
openclaw reset --scope config+creds+sessions --yes --non-interactive
```
