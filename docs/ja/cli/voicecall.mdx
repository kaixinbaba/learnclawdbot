---
summary: "`openclaw voicecall` の CLI リファレンス（voice-call プラグインコマンドサーフェス）"
read_when:
  - voice-call プラグインを使用していて CLI エントリポイントが必要な場合
  - `voicecall call|continue|status|tail|expose` の簡単な例が必要な場合
---

# `openclaw voicecall`

`voicecall` はプラグイン提供のコマンドです。voice-call プラグインがインストールされて有効になっている場合のみ表示されます。

主要ドキュメント：
- Voice-call プラグイン：[Voice Call](/plugins/voice-call)

## 共通コマンド

```bash
openclaw voicecall status --call-id <id>
openclaw voicecall call --to "+15555550123" --message "Hello" --mode notify
openclaw voicecall continue --call-id <id> --message "Any questions?"
openclaw voicecall end --call-id <id>
```

## Webhook の公開（Tailscale）

```bash
openclaw voicecall expose --mode serve
openclaw voicecall expose --mode funnel
openclaw voicecall unexpose
```

セキュリティ注意：信頼するネットワークにのみ Webhook エンドポイントを公開してください。可能であれば Funnel よりも Tailscale Serve を優先してください。
