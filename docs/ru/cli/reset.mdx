---
summary: "Справочник CLI для `openclaw reset` (сброс локального состояния/конфигурации)"
read_when:
  - Вы хотите очистить локальное состояние, сохранив установленный CLI
  - Вы хотите выполнить пробный запуск для проверки, что будет удалено
---

# `openclaw reset`

Сброс локальной конфигурации/состояния (CLI остается установленным).

```bash
openclaw reset
openclaw reset --dry-run
openclaw reset --scope config+creds+sessions --yes --non-interactive
```
