---
summary: "Справка CLI для `openclaw agents` (список/добавить/удалить/установить идентичность)"
read_when:
  - Вы хотите несколько изолированных агентов (рабочие пространства + маршрутизация + авторизация)
---

# `openclaw agents`

Управление изолированными агентами (рабочие пространства + авторизация + маршрутизация).

Связанное:
- Маршрутизация нескольких агентов: [Маршрутизация нескольких агентов](/concepts/multi-agent)
- Рабочее пространство агента: [Рабочее пространство агента](/concepts/agent-workspace)

## Примеры

```bash
openclaw agents list
openclaw agents add work --workspace ~/.openclaw/workspace-work
openclaw agents set-identity --workspace ~/.openclaw/workspace --from-identity
openclaw agents set-identity --agent main --avatar avatars/openclaw.png
openclaw agents delete work
```

## Файлы идентичности

Каждое рабочее пространство агента может включать `IDENTITY.md` в корне рабочего пространства:
- Пример пути: `~/.openclaw/workspace/IDENTITY.md`
- `set-identity --from-identity` читает из корня рабочего пространства (или явного `--identity-file`)

Пути аватаров разрешаются относительно корня рабочего пространства.

## Установка идентичности

`set-identity` записывает поля в `agents.list[].identity`:
- `name`
- `theme`
- `emoji`
- `avatar` (путь относительно рабочего пространства, URL http(s) или data URI)

Загрузить из `IDENTITY.md`:

```bash
openclaw agents set-identity --workspace ~/.openclaw/workspace --from-identity
```

Явно переопределить поля:

```bash
openclaw agents set-identity --agent main --name "OpenClaw" --emoji "🦞" --avatar avatars/openclaw.png
```

Пример конфигурации:

```json5
{
  agents: {
    list: [
      {
        id: "main",
        identity: {
          name: "OpenClaw",
          theme: "space lobster",
          emoji: "🦞",
          avatar: "avatars/openclaw.png"
        }
      }
    ]
  }
}
```
