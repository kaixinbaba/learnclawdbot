---
summary: "Справочник CLI для `openclaw uninstall` (удаление сервиса gateway + локальных данных)"
read_when:
  - Вы хотите удалить сервис gateway и/или локальное состояние
  - Вы хотите сначала выполнить пробный запуск
---

# `openclaw uninstall`

Удаление сервиса gateway + локальных данных (CLI остается).

```bash
openclaw uninstall
openclaw uninstall --all --yes
openclaw uninstall --dry-run
```
