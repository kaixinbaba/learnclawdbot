---
summary: "`openclaw sessions` CLI 레퍼런스 (저장된 session + 사용량 목록)"
read_when:
  - 저장된 session을 나열하고 최근 활동을 보고 싶은 경우
---

# `openclaw sessions`

저장된 대화 session을 나열합니다.

```bash
openclaw sessions
openclaw sessions --active 120
openclaw sessions --json
```
