---
summary: "`openclaw uninstall` CLI 레퍼런스 (gateway 서비스 + 로컬 데이터 제거)"
read_when:
  - gateway 서비스 및/또는 로컬 state를 제거하고 싶은 경우
  - 먼저 드라이 런을 원하는 경우
---

# `openclaw uninstall`

Gateway 서비스 + 로컬 데이터를 제거합니다 (CLI는 남음).

```bash
openclaw uninstall
openclaw uninstall --all --yes
openclaw uninstall --dry-run
```
