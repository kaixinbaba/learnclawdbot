---
summary: "`openclaw docs` CLI 참조 (실시간 문서 인덱스 검색)"
read_when:
  - 터미널에서 OpenClaw 실시간 문서를 검색하고 싶을 때
---

# `openclaw docs`

실시간 문서 인덱스를 검색합니다.

```bash
openclaw docs browser extension
openclaw docs sandbox allowHostControl
```
