---
summary: "`openclaw reset` CLI 레퍼런스 (로컬 state/config 리셋)"
read_when:
  - CLI를 설치된 상태로 유지하면서 로컬 state를 지우고 싶은 경우
  - 제거될 항목의 드라이 런이 필요한 경우
---

# `openclaw reset`

로컬 config/state를 리셋합니다 (CLI는 설치된 상태로 유지).

```bash
openclaw reset
openclaw reset --dry-run
openclaw reset --scope config+creds+sessions --yes --non-interactive
```
