---
summary: "浏览器自动化和 X/Twitter 发布的手动登录"
read_when:
  - 你需要登录网站进行浏览器自动化
  - 你想发布更新到 X/Twitter
---

# 浏览器登录 + X/Twitter 发布

## 手动登录（推荐）

当网站需要登录时，**在 host 浏览器配置文件（openclaw 浏览器）中手动登录**。

**不要**把你的凭据给模型。自动登录通常会触发反机器人防御，可能会锁定账户。

返回主浏览器文档：[Browser](/tools/browser)。

## 使用哪个 Chrome 配置文件？

OpenClaw 控制一个**专用的 Chrome 配置文件**（名为 `openclaw`，橙色调 UI）。这与你的日常浏览器配置文件是分开的。

访问它的两种简单方法：

1) **让 agent 打开浏览器**，然后你自己登录。
2) **通过 CLI 打开**：

```bash
openclaw browser start
openclaw browser open https://x.com
```

如果你有多个配置文件，传递 `--browser-profile <name>`（默认是 `openclaw`）。

## X/Twitter：推荐流程

- **读取/搜索/线程**：使用 **bird** CLI skill（无浏览器，稳定）。
  - 仓库：https://github.com/steipete/bird
- **发布更新**：使用 **host** 浏览器（手动登录）。

## 沙箱和 host 浏览器访问

沙箱浏览器会话**更可能**触发机器人检测。对于 X/Twitter（和其他严格的网站），优先使用 **host** 浏览器。

如果 agent 被沙箱化，浏览器工具默认使用沙箱。要允许 host 控制：

```json5
{
  agents: {
    defaults: {
      sandbox: {
        mode: "non-main",
        browser: {
          allowHostControl: true
        }
      }
    }
  }
}
```

然后定位到 host 浏览器：

```bash
openclaw browser open https://x.com --browser-profile openclaw --target host
```

或者为发布更新的 agent 禁用沙箱。
