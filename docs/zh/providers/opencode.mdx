---
summary: "使用 OpenCode Zen(精选模型)与 OpenClaw"
read_when:
  - 您想使用 OpenCode Zen 进行模型访问
  - 您想使用一个精选的编程友好型模型列表
---
# OpenCode Zen

OpenCode Zen 是由 OpenCode 团队推荐的**精选模型列表**,专为编程代理优化。
这是一个可选的托管模型访问路径,使用 API 密钥和 `opencode` provider。
Zen 目前处于测试阶段。

## CLI 设置

```bash
openclaw onboard --auth-choice opencode-zen
# 或非交互式
openclaw onboard --opencode-zen-api-key "$OPENCODE_API_KEY"
```

## 配置代码片段

```json5
{
  env: { OPENCODE_API_KEY: "sk-..." },
  agents: { defaults: { model: { primary: "opencode/claude-opus-4-5" } } }
}
```

## 注意事项

- 也支持 `OPENCODE_ZEN_API_KEY`。
- 您需要登录 Zen,添加计费详情,然后复制您的 API 密钥。
- OpenCode Zen 按请求计费;查看 OpenCode 仪表板了解详情。
