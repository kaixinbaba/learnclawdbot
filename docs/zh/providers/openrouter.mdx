---
summary: "使用 OpenRouter 的统一 API 在 OpenClaw 中访问多种模型"
read_when:
  - 你想用一个 API key 访问多个 LLM
  - 你想在 OpenClaw 中通过 OpenRouter 运行模型
---
# OpenRouter

OpenRouter 提供了一个**统一 API**，将请求路由到单个端点和 API key 背后的多个模型。它兼容 OpenAI，所以大多数 OpenAI SDK 只需切换 base URL 即可工作。

## CLI 设置

```bash
openclaw onboard --auth-choice apiKey --token-provider openrouter --token "$OPENROUTER_API_KEY"
```

## 配置片段

```json5
{
  env: { OPENROUTER_API_KEY: "sk-or-..." },
  agents: {
    defaults: {
      model: { primary: "openrouter/anthropic/claude-sonnet-4-5" }
    }
  }
}
```

## 注意事项

- 模型引用格式为 `openrouter/<provider>/<model>`。
- 有关更多模型/提供商选项，请参见 [/concepts/model-providers](/concepts/model-providers)。
- OpenRouter 在底层使用带有你的 API key 的 Bearer token。
