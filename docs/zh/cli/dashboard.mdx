---
summary: "`openclaw dashboard` 的 CLI 参考（打开控制 UI）"
read_when:
  - 您想使用当前令牌打开控制 UI 时
  - 您想打印 URL 而不启动浏览器时
---

# `openclaw dashboard`

使用您当前的认证打开控制 UI。

```bash
openclaw dashboard
openclaw dashboard --no-open
```
