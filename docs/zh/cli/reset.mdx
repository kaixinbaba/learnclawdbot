---
summary: "`openclaw reset` 的 CLI 参考（重置本地状态/配置）"
read_when:
  - 您想要清除本地状态但保持 CLI 已安装
  - 您想要模拟运行查看将被删除的内容
---

# `openclaw reset`

重置本地配置/状态（保持 CLI 已安装）。

```bash
openclaw reset
openclaw reset --dry-run
openclaw reset --scope config+creds+sessions --yes --non-interactive
```
