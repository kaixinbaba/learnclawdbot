---
summary: "`openclaw onboard` 的 CLI 参考（交互式入门向导）"
read_when:
  - 您想要获得网关、工作空间、认证、频道和技能的引导式设置
---

# `openclaw onboard`

交互式入门向导（本地或远程网关设置）。

相关：
- 向导指南：[入门](/start/onboarding)

## 示例

```bash
openclaw onboard
openclaw onboard --flow quickstart
openclaw onboard --flow manual
openclaw onboard --mode remote --remote-url ws://gateway-host:18789
```

流程说明：
- `quickstart`：最少的提示，自动生成网关令牌。
- `manual`：端口/绑定/认证的完整提示（`advanced` 的别名）。
- 最快的首次聊天：`openclaw dashboard`（控制 UI，无需频道设置）。
