---
summary: "`openclaw tui` 的 CLI 参考（连接到网关的终端 UI）"
read_when:
  - 您想要一个连接到网关的终端 UI（支持远程）
  - 您想要从脚本传递 url/token/session
---

# `openclaw tui`

打开连接到网关的终端 UI。

相关：
- TUI 指南：[TUI](/tui)

## 示例

```bash
openclaw tui
openclaw tui --url ws://127.0.0.1:18789 --token <token>
openclaw tui --session main --deliver
```
