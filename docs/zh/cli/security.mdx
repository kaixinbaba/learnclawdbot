---
summary: "`openclaw security` 的 CLI 参考（审计并修复常见的安全陷阱）"
read_when:
  - 您想要对配置/状态进行快速安全审计
  - 您想要应用安全的"修复"建议（chmod、收紧默认值）
---

# `openclaw security`

安全工具（审计 + 可选修复）。

相关：
- 安全指南：[安全](/gateway/security)

## 审计（Audit）

```bash
openclaw security audit
openclaw security audit --deep
openclaw security audit --fix
```

当多个私信（DM）发送者共享主会话时，审计会发出警告，并建议为共享收件箱使用 `session.dmScope="per-channel-peer"`（或对于多账户频道使用 `per-account-channel-peer`）。
当小型模型（`<=300B`）在没有沙盒化的情况下使用，并且启用了 web/browser 工具时，它也会发出警告。
